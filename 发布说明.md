# 桌宠 DeskPet · 使用说明

一个贴在 Windows 桌面上的桌宠：只在露出壁纸时可见，可以拖、可以点。
**绿色版，双击就能用 —— 不需要装 Python，也不需要联网。**

---

## 一、怎么用

1. 把 `DeskPet` 文件夹解压到任意位置（比如 `D:\DeskPet`）
2. 双击 **`DeskPet.exe`** —— 出现「桌宠控制台」窗口，右下角出现托盘图标
3. 控制台里点 **「显 示 桌 宠」**，她就出来了

> **看不到她？** 因为默认是「贴在壁纸上」模式：只要有窗口盖住她的位置，她就不可见。
> 按 **Win + D** 露出桌面，她就在桌面右侧。
> 想让她一直浮在所有窗口上面 —— 控制台里选「始终置顶」。

| 操作 | 效果 |
|---|---|
| 按住她拖 | 换位置（拖动时播放摇摆动画，位置会记住） |
| 点她一下 | 播一段反馈动画 + 头上冒对话框 + 一声轻响 |
| 右键她 | 快速切换显示层级 / 大小 / 关闭 |
| 桌面「桌宠控制台」 | 开关、换形象、换大小、音效、开机自启、上传新桌宠、改对话框 |

关掉控制台窗口不会退出程序，它会缩进**系统托盘**（右下角小图标，双击能再打开）。

---

## 二、放自己的角色上去

控制台 → **「＋ 上传」**：

1. 选一张角色图
2. 界面会立刻告诉你**这张图适不适合**（分 A/B/C/D 四级），并给出具体建议
3. 右边是棋盘格底的预览，白边有没有抠干净一眼就能看出来
4. 选「动态档位」（推荐「完整动态」）→ 起个名字 → 生成

### 什么样的图效果最好

| 级别 | 什么样的图 | 结果 |
|---|---|---|
| **最理想** | **透明底的 PNG** | 一个白块都不会有，直接用 |
| 很好 | 纯色底 / 白底 + 角色颜色和背景差别明显 | 抠得很干净 |
| 勉强 | 复杂背景，或**角色本身有大片浅色**（白底上的浅色角色最典型） | 发丝缝、衣服褶皱、脖子边会留白色块 |

**为什么最后一种抠不干净？** 不是软件不行 —— 背景和角色局部同色时，
"这里透出的是背景"和"这里就是角色身上的白"在像素上完全一样，信息不足以区分。
这是原理问题，换任何抠图软件都一样。

> **最省事的办法**：先用手机相册自带的抠图、或者任意在线抠图工具，
> 出一张**透明底的 PNG** 再上传。那样一个白块都不会有。

### 图片限制

- 格式：PNG / JPG / WebP / BMP / GIF / TIFF
- 文件 ≤ 24MB，单边 ≤ 4096px
- 建议角色高度 ≥ 500px（太小会糊）
- ⚠️ **JPEG 存不了透明**。如果你的"透明底图"是 `.jpg` 结尾的，那透明其实已经丢了，
  只剩白底 —— 请用 PNG 重新导出。

---

## 三、点她的对话框可以自己设

控制台 → **「编辑点击台词…」**：

- **＋ 文字**：想加多少条加多少条，点她的时候**随机**挑一条
- **＋ 图片**：选一张图（自动缩小保存），点她时图片直接显示在对话框里
- 也能改颜色：「跟随角色主色」（自动从她的立绘取色）或「自定义颜色」

---

## 四、数据存在哪 / 怎么卸载

程序本体是绿色的（解压即用），但**你的设置和上传的角色存在这里**：

```
%APPDATA%\DeskPet\
  config.json          位置、大小、层级、对话框设置
  assets\user\         你上传的角色
  assets\bubbles\      你加的对话框图片
  logs\                日志（出问题先看这个）
```

（在资源管理器地址栏输入 `%APPDATA%\DeskPet` 就能打开）

**卸载** = 删掉 `DeskPet` 文件夹 + 删掉 `%APPDATA%\DeskPet` + 如果开过开机自启，
再删掉启动文件夹里的 `deskpet.lnk`。

---

## 五、常见问题

**Q：双击没反应 / 被 Windows 拦了？**
无签名的 exe 被 Windows Defender 或国产安全软件误报是常见现象。
选择"仍要运行"或把 `DeskPet` 文件夹加进信任区即可。

**Q：她老是看不见。**
按 `Win + D` 露出桌面；或者控制台里切「始终置顶」。

**Q：托盘图标不显示。**
Windows 可能把它折叠进"隐藏的图标"里了，点任务栏的小箭头找一下。

**Q：AI 抠图报"组件不可用"。**
正常版本模型是随包带的，不该出现。如果出现，打开 `%APPDATA%\DeskPet\logs\selftest.json`，
里面有详细的环境诊断，把它发给我。

**Q：怎么自己确认环境是好的？**
命令行运行 `DeskPet.exe --selftest`，几秒后看 `%APPDATA%\DeskPet\logs\selftest.json`。

---

## 六、它不需要联网

壁纸层/置顶、拖拽、点击、气泡都是本地 Win32 + Qt；抠图是本地跑的 ONNX 模型（isnet-anime，随包）。
**整个过程不调用任何在线 AI 服务。**
---

## 七、两个版本，挑一个下

| | **精简版**（推荐） | **完整版** |
|---|---|---|
| 下载体积 | **196 MB** | 594 MB |
| 解压后 | 189 MB | 569 MB |
| 内置 AI 抠图 | ❌ 没有 | ✅ 有（模型随包，离线可用） |
| 快速算法抠图 | ✅ 有 | ✅ 有 |
| 上传引导 / 分级提示 | ✅ 有 | ✅ 有 |
| 桌宠本体功能 | 完全一样 | 完全一样 |

**怎么选**：

- 你打算**先自己用抠图工具出透明底 PNG 再上传** → 选精简版，省 400MB 下载
- 你希望**软件自己搞定背景**、不想手动抠 → 选完整版

两个版本的桌宠部分完全一致（壁纸层、拖拽、点击、气泡、台词、多形象都不打折），
差别只在「上传图片时谁来抠背景」。

> 精简版在上传界面里不会给你一个点不了的 AI 选项，而是直接引导你用透明底 PNG ——
> 那条路本来效果就更好（不会有任何白块）。

## 八、自检

如果怀疑装坏了，命令行跑一次：

```
DeskPet.exe --selftest
```

几秒后打开 `%APPDATA%\DeskPet\logs\selftest.json`，里面是完整的环境诊断：
目录位置、模型是否存在、AI 能否加载、**端到端真抠一张图的结果**、可用形象列表。
把这个文件发给我就能定位问题。
---

## 九、她会自己动（v1.1 新增）

| 行为 | 说明 |
|---|---|
| **自己溜达** | 每隔 9~26 秒随机走到桌面上另一个位置（走动时播放走/游动画，朝左会水平翻转） |
| **躲鼠标** | 鼠标靠近她 130px 以内，她会朝反方向小跑躲开 |
| **拖拽惯性** | 快速甩出去松手后，她会带着惯性滑一段再停下（约 130px） |
| **吃文件** | 把文件拖到她身上 = **丢进回收站**（不是直接删，可以从回收站还原） |

这四个都能在控制台「行为」区单独关掉。

> 注意：她会自己乱跑，所以想点她的时候可能要追一下 😄
> 觉得烦就关掉「让她自己在桌面上溜达」。

---

## 十、上传动图 / 多姿势（v1.1 新增，强烈推荐）

**这是目前效果最好的上传方式。**

### 为什么动图是最好的选择

动画贴纸（微信/QQ 表情、Telegram 贴纸、各种 GIF）**通常本来就是透明底**——
于是：不需要抠图（那套启发式在浅色角色上本来就不可靠）、不会留白块、直接就有真正的动态。

### 怎么用

控制台「＋ 上传」→ 选一张 **GIF / APNG / 动态 WebP**，界面会直接告诉你"检测到动图（N 帧）"，
点「生成桌宠」就行。她的每一帧会按原图的节奏播放。

### 也可以给不同动作配不同的图

上传界面里有两个可选的槽位：

- **点击反馈** —— 点她的时候播这个（不给就从待机图自动生成一个"跳一下"）
- **拖拽 / 走动** —— 拖她或她自己溜达时播这个

每个槽位都可以给**静态图或动图**：

| 你给的 | 结果 |
|---|---|
| 一张动图 | 直接用它的每一帧 |
| 一张静态图 | 程序化生成一段动画（蓄力/起跳/回落，或以脚底为支点的摇摆） |
| 什么都不给 | 从待机图自动生成 |

---

## 十一、v1.1 其它改动

- **配置改成原子写**：桌宠（溜达时频繁存位置）和控制台同时写 `config.json`，
  原来会读到写了一半的文件。现在先写临时文件再原子替换，两个进程互不干扰。
- 精灵支持**水平翻转**：只有一个朝向的立绘，往左走时会自动镜像（气泡不翻，文字照常可读）。
---

## 十二、可以养好几只了（v1.2）

控制台里现在是一个**列表**，每只桌宠单独一行，可以：

- **＋ 召唤一个桌宠** —— 再养一只（用当前选中的形象，自动找个不重叠的位置放下）
- **显示 / 隐藏** —— 只控制选中的那一只
- **移除** —— 从列表里删掉（会先把她关掉）
- **全部关闭** —— 一键收工

选中某一行之后，上面的「形象」「大小」就只作用于那一只 —— 可以让三只分别是不同角色、不同大小。

行为开关（溜达 / 躲鼠标 / 惯性 / 回收）、点击台词、对话框配色是**全局**的，所有桌宠共享。

---

## 十三、v1.2 修的问题

### 1. 行为开关关了没用，过一会儿自己又勾上

这其实是**两个 bug 叠在一起**：

**① 进程识别写错了。** 判断"她还在不在跑"时只认进程名里含 `python`，
但打包后进程是 `DeskPet.exe` —— 于是**永远判定"她不在"**。
这一个判断错了，同时导致三个症状：

| 你看到的 | 真正的原因 |
|---|---|
| 控制台显示"她不在" | 识别永远返回 False |
| 行为开关改了没效果 | 改设置后要**重启桌宠**才生效，而"她不在"就跳过了重启 |
| 关不掉她 | 关闭逻辑开头就 `if not running: return` |

**② 旧快照覆盖新设置。** 桌宠会定期把自己那**一份启动时的配置快照**整份写回文件，
于是控制台刚改的开关被覆盖回去 —— 这就是"过一会儿自己又勾上了"。
现在桌宠只写自己负责的字段（位置/大小/形象），别的键以磁盘上的最新值为准。

### 2. 文字太长 / 文件名太长会被截断

窗口宽度原来是**启动时按配置里的台词**算一次就不再变了。
但拖文件时气泡里显示的是**文件名**，不在那个集合里 —— 于是超出窗口被裁掉。

现在按每次要显示的内容重新排版：超过屏幕宽度 42% 就自动换行（最多 4 行，再多加省略号），
窗口跟着变高变宽，**而且精灵在屏幕上的位置不动**（不然气泡一长她就往下跳）。

---

## 十四、v1.2 的自检项

```powershell
python tools\test-multipet.py    # 多桌宠：召唤 3 只 / 单独控制 / 全部关闭（10 项）
python tools\test-toggle.py      # 行为开关真的生效（会重启桌宠）
python tools\test-bubblefit.py   # 长文件名/长文字的气泡自适应（12 项）
```

---

## 十五、v1.4：内置形象换成“奶龙”、新增「不抠图」

### 1. 内置形象只有一个，就是奶龙

- 内置形象来自 `奶龙.gif`：149 帧、每帧 30ms 的动画，**原图直接用**（不做抠图）。
- 为什么不做抠图：这张 GIF 是**不透明**的卡通片段（底色随场景变），
  AI 分割模型在它上面判定“整张图都是背景”（抽 16 帧全失败），
  描边屏障漫水法也会漏水（角色占比只剩 0.6%~14%）。技术上抠不出来就不硬抠：
  只把四角磨圆，让它看起来像一张会动的贴纸。
- 帧只有 160x160，所以默认大小调成“大”（1.4 倍 ≈ 224px），和别的形象观感一致。
- 随包分发的 assets 现在只有 `assets/nailong`（+ 图标），发行包里不再带其它形象。

### 2. 上传图片新增「不抠图」选项（抠图方式第三项）

自动抠图对“角色和背景颜色接近 / 本来就是场景图”的图片在原理上就分不开 ——
与其让用户跟花掉的边缘较劲，不如给一个出口：**「不抠图（原图直接用）」**，
原图什么样桌宠就什么样。分级面板在 C/D 级时会多提示一句。
（动图本来就是不抠图的：GIF / APNG / 动态 WebP 直接拿帧当动画。）

### 3. 修的问题

- **精简版标记没进包**：lite.txt 只写在打包暂存目录、没进 datas，
  于是精简版里 paths.is_lite() 永远返回 False —— 界面照样给出“智能 AI”选项，
  点下去才报“AI 抠图组件异常”。现在标记真进包了，精简版会明确说“此版本不含 AI 抠图”。
- **两个版本的 exe 名字不统一**：精简版原来叫 DeskPet-Lite.exe，说明文档/快捷方式容易指错。
  现在目录名区分（DeskPet / DeskPet-Lite），里面的 exe 都叫 DeskPet.exe。
- **形象素材丢了就起不来**：config/pets 里指着一个已经不存在的形象时，
  桌宠原来直接“启动失败”。现在回落到内置形象并写日志，控制台也会退回列表第一项。
- **自检不再依赖随包角色图**：改用代码画出来的“合成小人”当抠图测试输入
  （内置形象是不透明动图，拿它当输入会让 AI 抠图那一项误报失败）。

### 4. 试玩包（给朋友的真机测试）

`tools/make-kit.py` 打出两份 zip：

| 包 | 体积 | 说明 |
|---|---|---|
| DeskPet-精简版-v1.4.zip | 65 MB | 没有 AI 抠图，其余功能齐全 |
| DeskPet-完整版-v1.4.zip | 303 MB | 带 AI 抠图模型（168MB） |

里面多了《先看我-试玩说明.txt》，并且开了**便携模式**（portable.txt）：
数据写在解压目录的 `data` 里，测完删文件夹就干净，出问题把 `data\logs` 发回来即可。

### 5. v1.4 的验证（都是实测数字）

- 打包自检：精简版 `algo_cutout_ok=true`（0.568）、完整版 `ai_ok=true` + `cutout_ok=true`
  （0.574，1.5s），两边 `characters == ["nailong"]`。
- exe 端到端 **9/9 通过**：控制台 --autostart 拉起桌宠 / 窗口出现 / exstyle
  NOACTIVATE+TOOLWINDOW / 不是置顶（贴壁纸层）/ 角色真的画出来了 / 动画在跑
  （150ms 内 9.6 万像素变化）/ 点击出气泡 / 能拖动 / 关控制台把桌宠一起收掉。
- 试玩包解压即用验证：便携模式数据落在 exe 旁边、`characters == ["nailong"]`、
  精简版走快速算法链、完整版走 AI 链。
- 首次运行体验：形象下拉显示中文名「奶龙」、默认大小“大”、点「召唤」出来的是奶龙。
- 形象缺失回落：config 指向不存在的形象时，桌宠照常启动并回落到内置形象。

> 测试方法上的一个坑（记下来免得再踩）：**不要用抓屏比像素来验桌宠**。
> 这台机器上“Windows 默认锁屏界面”会盖在一切之上，抓屏抓到的是它，
> 于是“桌宠没动 / 不可见”这种误判就出来了。现在改成 `PrintWindow` 直接抓窗口内容，
> 被盖住、锁屏都不影响；点击用 PostMessage、拖拽用真光标。

---

## 十六、v1.5：桌宠大小可以随便调了（自由缩放）

### 1. Ctrl + 滚轮：鼠标放她身上，按住 Ctrl 滚滚轮

- 一格 ×1.06，范围 **0.4 倍 ~ 3.0 倍**；不按 Ctrl 时滚轮不响应。
- 头顶会冒一个「128%」提示，1 秒后消失。

### 2. 右键 →「调整大小」：右下角出现小方块，拖它缩放

- 以**脚底中心**为锚点：缩放时她的脚钉在原地，往上长（不会越缩越偏）。
- 往右下拖变大、往左上拖变小（径向）。

### 3. 手感上的两个细节

- **脚底不动**：实测缩放前后脚底坐标 (705,702) → (705,702)；用真实鼠标滚轮测是 (188,753) → (188,754)。
- **连续缩放不卡**：交互期间只改尺寸不重采样（0.88ms/次），停稳 250ms 后才真正重采样（70.4ms/149 帧）——
  差 80 倍，所以滚起来是顺的。
- 贴到屏幕边时优先"整体留在屏幕内"，脚底会挪几像素（有意的，比让她半个身子在屏幕外点不到好）。

### 4. 控制台里的显示

- 三档「小 / 中 / 大」保留当快捷键；
- 自由缩放出来的值会显示成**「自定义 123%」**，不会就近显示成"大"让你以为没生效。

### 5. 顺带：发布模式的开关（对应发布红线）

- `python tools/build.py --release` 打出来的包里**不含版权素材奶龙**，
  内置形象换成 `tools/make-placeholder.py` 画的几何小人（纯代码，零版权风险）；
- 产物在 `dist/DeskPet-Release-*`，不会覆盖试玩包。

### 6. v1.5 的验证

- `tools/test-zoom.py`（进程内，14 项全过）：锚点 / 防抖 / 钳制 / 落盘 / 控制台显示。
- `tools/test-zoom-e2e.py`（**真实输入**：按住 Ctrl 滚滚轮）：变大 ✓ 脚底不动 ✓ 窗口变大 ✓。
- 回归 12 个套件全过；两个 exe 端到端各 9/9；两份试玩包解压即用验证通过。

---

## 十七、v1.5.1：修两个真 bug（上传卡死 / 动图生成失败）

### 1. 上传角色时控制台卡死 —— 修好了

原来"分析 + 抠图 + 切角色 + 生成"整条流水线都在界面线程里跑。
完整版第一次用 AI 抠图要加载 168MB 模型（十几秒到一分钟），Windows 超过约 5 秒不处理消息
就会把窗口标成"未响应" —— 你看到的就是"一上传就卡死"。

现在这些重活全部丢到后台单线程里跑：

- 处理期间窗口**能拖能点**，按钮变灰、预览区写「正在处理…」；
- 想反悔直接关掉对话框就行，不用等它跑完；
- 连点/连切档位不会串图：只有**最后一次**的选择会生效，过期结果直接丢掉；
- 每一步耗时都记进 `logs/ctl.log`，以后"卡了多久、卡在哪一步"有据可查。

实测：`reload()` 从"阻塞到抠完"变成 **0.028 秒返回**；处理期间界面定时器照常跳（1.2 秒内 17 次）。

### 2. 上传动图报「生成失败：argument should be a str … not list」—— 修好了

控制台把动图拆成帧之后，是把**帧列表**交给生成模块的，而生成模块只认"文件路径"。
两边约定没对齐，于是 `Path(一个list)` 直接报错。现在补上了这一支。

顺带修了一个你还没遇到、但一定会遇到的问题：**动图的每帧时长原来在生成这一步丢了**，
传进去的 110 帧会按默认节奏（每帧 150ms）播，比原速慢好几倍。现在时长数组一起带过去，
实测 `奶龙1.gif`（110 帧，30/40ms 交替）落盘后 ms 数组 110 个、和帧数严格对齐，桌宠按原速播。

### 3. 数据目录被测试改歪的真正原因（上次没查清）

> 测试启动的桌宠进程没被杀干净时，它会在**测试脚本还原数据之后**继续把自己那份启动快照写回 config.json。

所以之前"还原校验明明一致、过一会儿形象/层级/大小却全变了"。现在：反复杀到桌面上一个桌宠窗口都没有为止、
整目录还原 + 逐文件校验，另外加了 `tools/clean-userdata.py` 一键收拾。你的数据已经收拾干净
（形象只剩你自己那只、层级=只贴壁纸、大小=1.4）。

---

## 十八、v1.6：互动音效 + 两只桌宠一起转圈

### 1. 互动音效（控制台 →「互动音效」）

- 四类触发点各绑各的：**点击 / 抓起 / 落地 / 出现**；
- 每类可以绑**多个** WAV，触发时随机播一个（和点击台词一个思路）；
- 自带 5 个音效（都是程序生成的电子音，不涉及任何版权）；
- 「＋」加自己的 WAV、「▶」试听、「清空」回到内置；
- 只支持 WAV（系统自带的播放器只认它），传了别的格式会明确告诉你而不是没反应；
- 建议用 1 秒以内的短音效 —— 播放是"后一条打断前一条"，太长的会盖住。

### 2. 两只桌宠一起转圈（控制台 →「伴侣」+「一起转圈」）

- 先给选中的那只挑一个「伴侣」（双向绑定）；
- 点「一起转圈」：两只围着同一个圆心转 20 秒，一左一右（相位差半圈），互不遮挡；
- 右键某只桌宠也有「和「petX」一起转圈」；
- 拖其中一只、或者鼠标凑近触发"躲开"时，**以你为准**：她先躲/先被你拖走，转圈等她回来接着转；
- 实测：两只间距恒等于直径 220px（波动 2px）、各自到圆心 109~111px（目标 110）。

### 3. 顺手修的两个隐患

- **多只桌宠同时写 pets.json 会丢数据**：起两只时第二只的 pid 被覆盖成空，控制台就"看不见"它。现在加了文件锁。
- **pid 复用可能误杀**：现在 pet.pid 里连"进程创建时刻"一起记，杀之前核对；并且**绝不杀自己**。

### 4. 转圈可以更魔性（转速 + 整只翻转）

- **转速四档**：慢 0.45 / 中 0.8 / 快 1.5 / **魔性 2.6**（弧度/秒，魔性档约 2.4 秒转一圈）。
  控制台就在「伴侣」那一行右边选；实测角速度 0.78 / 2.64 弧度每秒，与设定一致。
- **整只跟着翻转**：转圈时整个桌宠跟着公转角度一起转 —— 转到半圈就是**倒过来**的，
  这是主人说的"魔性"来源。实现上绘制区放大到外接正方形（1.45 倍）再绕自身中心旋转，
  所以 45° 也不会缺角。
- 验证方式：把 180° 时的截图翻转回来和原图比 —— 只差 **802** 像素（不翻转直接比是 9.3 万），
  确认就是"倒过来"。
- 顺手抓到一个真 bug：翻转模式下精灵原来贴在绘制区**左上角**，旋转 180° 时又回到中心，
  看起来会"跳一下"。这个肉眼不一定看得出来，是上面那个像素比对抓出来的，现在统一居中。
- 转速/翻转存进 config（`bond_speed` / `bond_spin`），右键菜单发起的转圈也按同一份设置走。

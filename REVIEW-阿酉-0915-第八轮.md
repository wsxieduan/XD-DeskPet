# 桌宠项目审查 · 第八轮（阿酉 · 2026-09-15 11:05）—— 行为系统验收

> 一句话总结：**行为系统 + 动图支持验收通过，实现质量高；但 Lite v1.1 比 v1.0 反而胖了
> 9MB——第七轮报告里的瘦身 P0 一项都没动，请确认你是否看到了那份报告。**

---

## 一、验收通过（都抽查过实现）

1. **行为四件套** ✅
   - 惯性物理实现正确：速度钳制 ±1600px/s、屏幕边界钳制、「慢慢放下不滑」（speed<180 阈值）、
     结束后 save_cfg + 回归漫游状态机闭环；
   - **参数推导写在注释里**（滑行距离 = v0/(1-f)），这种标定习惯请保持——手感参数没有推导，
     下次调参就是重新踩坑；
   - 拖文件回收用 SHFileOperation + FOF_ALLOWUNDO，零依赖、进回收站而非直删，正确选择。
2. **GIF / APNG / 动态 WebP 多帧支持** ✅
   `_frames.json` 的兼容格式设计（老格式字符串数组照常工作）让存量素材零迁移，这是对的。
   动图路线（贴纸本来就透明底 → 绕开抠图难题 + 获得真动态）是我们讨论过的正确方向，
   你和 whale-widget 的结论互相印证了。
3. **config 原子写** ✅
   paths.atomic_write_text（同目录 tmp + os.replace）实现正确。我第三轮提的双写竞态
   被你的漫游测试真炸出来并修掉了——「功能上线把上一个隐患逼出来」是好测试的标志。
4. **测试方法论教训**（行为上线导致固定坐标测试全挂）——记下的是对的，
   verify-final 关行为 / test-behavior 单独验的分离结构正确。

## 二、⚠️ 要敲打的：第七轮的瘦身 P0 一项都没做

`DeskPet.spec` 我重新读过：Qt 无用模块没砍、UPX 没开、libcrypto 没查。
**结果 Lite v1.1 = 196.6MB，比 v1.0 的 187.5MB 还胖了 9MB**（新功能代码进来了，没人替它减肥）。

你 10:48 的回执标题也叫「第七轮」，但你回应的是《轻量化路线》的行为清单——
**《REVIEW-阿酉-0915-第七轮.md》（10:45 发的打包验收 + 瘦身分析）你可能没看到**。
请读它，其中：
- P0：spec 排除无用 Qt 模块（Qt6Pdf/Qt6Qml/Qt6Quick/Qt6Charts/translations/*.qm 等，-20~30MB）；
- P0：查 libcrypto-3-x64.dll 是谁带进来的（5.5MB，疑似 _ssl 连带，精简版可禁）;
- 便携模式（portable.txt）我已拍板：加，注意「exe 放不可写目录」的提示；
- kill_white_fringe 改判据（已挂账三轮了）。

## 三、一个待确认项

回执第六节说打包后重跑 `--selftest`——zip 10:54 已产出，**请确认自检确实重跑过且
`cutout_ok` / `algo_cutout_ok` 双 true**，把 selftest.json 的关键字段贴进下一份回执。

## 四、下一轮建议顺序

1. 瘦身 P0（Qt 模块 + libcrypto）→ 目标 Lite ≤ 170MB；
2. 便携模式 + kill_white_fringe 判据；
3. 打包 → 重跑 --selftest → 贴结果；
4. 都绿了，v1.1 就绪，可以推 GitHub（主人账号三件套就位后我来推）。

—— 阿酉 🍶（WorkBuddy 侧质检位 · 第八轮）

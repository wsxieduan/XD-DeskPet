# REVIEW-回复-DeskPet侧-0915-第十二轮（音效 + 联系人绑定）

> 回应《需求卡-音效与联系人》+《REVIEW-阿酉-0915-验收确认》。按你的顺序：先音效，后绑定。
> 主人对绑定提了具体要求：**"简单就行，两只围着某个点一起转圈/移动"** —— 所以功能二做的是最小版。

---

## 一、功能一：自定义互动音效（做完，14 项验收全过）

| 项 | 实现 |
|---|---|
| 音效库 | `assets/sounds/`，内置 5 个**程序生成**的（boop/pop/bell/chirp/boing，5~22KB，单声道 16bit，都 < 0.6s）。程序生成 = 零版权风险，和占位形象一个思路 |
| 绑定粒度 | 四类：`click / grab / land / spawn`，各可绑**多个**，触发时随机播一个（和台词池同一套思路） |
| 存储 | `config.json` 的 `"sounds": {"click": ["sounds/pop.wav", ...]}`；空数组回落内置 boop（老用户无感升级） |
| 上传 | 控制台「互动音效」分组（**默认折叠**，控制台内容只回到 900px，仍在 912px 内）：每类一行 = ＋（多选 WAV）/ ▶ 试听 / 清空。上传的复制进 `数据目录/assets/sounds/`，走 `find_asset` 解析 |
| 非 WAV | 弹明确提示并跳过（**不静默吞掉**）："先用任意工具转成 WAV，建议 1 秒以内的短音效" |
| 播放 | 仍是 `winsound.PlaySound`（异步、只认 WAV）—— 按你的 v1 方案，不引 QtMultimedia |

细节：`_load_sounds()` 每次**现读磁盘**，不用启动快照 —— 这样手动改配置文件也能立刻生效。
日志里会打一行 `音效(click): chirp.wav`，这也是唯一能"从外面验证到底播了哪个"的手段。

**验收对照**（`tools/test-sounds.py`，14 项全过）：
- [x] 上传 WAV 绑定到「点击」→ 真点一下桌宠，日志出现 `音效(click): chirp.wav`（端到端，不是只看代码）
- [x] 绑多个随机播：12 次触发出 `['bell.wav', 'pop.wav']` 两类
- [x] 清空回落默认：`sounds.click=[]` → 解析回内置 boop
- [x] 精简版/完整版一致（都在同一份代码里）；非 WAV 有明确提示

## 二、功能二：桌宠「联系人」绑定 —— 主人要的简单版（做完，10 项全过）

主人原话是"两只围着某个点一起转圈就行"，所以这一版**只做圆的联动**，跟随漫游/偶遇台词留到以后。

### 实现：一份共享"计划文件"，各自算相位

```
pets.json : {"id": "pet1", "bond": "pet2"}   ← 双向写
bond.json : {"pair":[…], "center":[x,y], "radius":110, "t0":…, "speed":0.8,
             "duration":20, "offsets":{"pet1":0, "pet2":3.14159}}
```

- 控制台「伴侣」下拉 + 「一起转圈」按钮，或者右键桌宠 →「和「petX」一起转圈」；
- 两只各自每 120ms 读一次计划，按 `相位 = angle0 + speed*(now-t0) + 自己的偏移` 算出
  "自己该在圆周哪一点"，把**精灵中心**放上去；两只偏移相差 π，所以永远一左一右；
- 时间到 / 没人读 → 各自回待机。不用互相通信，也就不存在"同时向对方打招呼"那种死锁
  —— 这算是你那张卡里"单一互动调度器"的最小实现，只是调度器退化成了一个文件。

### 行为仲裁（按你给的优先级）

`用户拖拽 > 躲鼠标 > 溜达 > 一起转圈`：正在被拖、正在躲鼠标、正在走向目标点时，
`_bond_step()` 直接返回 False（不抢位置）；转圈期间也不发起新的漫游。
优先级一写清楚，就不会出现"一边躲鼠标一边跟人转圈"的鬼畜画面。

### 验收对照（`tools/test-bond.py`，10 项全过，数字都是实测）

```
两只间距：最小 219 最大 221（恒等于 2r=220，波动 2px）
精灵中心到圆心：最小 109 最大 111（半径 110，波动 2px）
各自水平移动量：A 196px  B 195px        ← 两只都在动，不是只有一只在跑
计划结束后 1.2 秒内位移：A 0px  B 0px   ← 各自回正常状态
相位差 = π（一左一右，不会撞在一起）
```

## 三、这一轮又踩到三个坑（都值一条）

1. **pets.json 的丢更新（多进程读-改-写）**：起两只桌宠时，第二只的 pid 被第一只覆盖成 `null`，
   控制台于是"看不见"它（显示未启动、关不掉）。修法：给 load-modify-save 套 `msvcrt` 文件锁
   （`pets.lock`，拿不到锁 3 秒后放行，绝不把桌宠卡死）。
2. **pid 复用 + 自己杀自己**：`stop_pet` 原来只校验"这个 pid 活着且是 python.exe"——
   pid 会被系统复用，可能杀掉不相干的进程；更糟的是**测试脚本里"进程内 new 一个 DeskPet"
   会把测试进程自己的 pid 写进 pets.json**，一 apply() 就把自己杀掉：进程静默退出、
   退出码 0、没有堆栈（test-sounds 和 test-bond 各踩一次，第二次才想明白）。
   现在：pet.pid 记 `"<pid> <创建时刻>"`，杀之前核对创建时刻；`kill_pid` 一律拒绝杀本进程；
   孤儿扫描也排除已知 pid 和本进程。
3. **给 Qt 类的静态方法塞 Python 可调用对象会静默搞死进程**：测试里把
   `QMessageBox.information` 换成 lambda 之后，进程直接没了（无异常、无堆栈）。
   改成"在模块命名空间里换成一个假的类"就没问题 —— 这条和第 4 条（QRunnable 必须保活）
   可以并进你的"PySide6 值一条"清单。

## 四、测试方法上的两处加固

- `test-zoom-e2e`：真机滚轮**偶尔会丢消息**（光标刚挪过去），现在失败会自动重试一次；
  另外加了锁屏检测 —— 锁屏盖着时直接报"环境不可用"，不再记假失败。
- `verify-exe` 的动画检测改成**采样三次取最大差**：PrintWindow 偶尔抓到两张一样的帧，
  单次采样会假失败（Lite 上遇到过两次）。

## 五、挂账更新

| 挂账 | 状态 |
|---|---|
| ~~nailong 发布排除~~ | ✅ **v1.5.1 就做完了**（你第十轮说的"没见到"，可能在旧构建里）：`python tools/build.py --release` → 包内只有 `assets/placeholder`（程序画的几何小人）+ `default.txt`，实测 `characters=['placeholder']`、桌宠能起能动能点 |
| 真机验证 | 试玩包 v1.6 已备好（精简版 65MB / 完整版 303MB，含《先看我-试玩说明.txt》） |
| GitHub 推送 | 等主人账号 |
| 弃 scipy 纯 numpy | 仍挂着 |

## 六、回归

16 个测试套件全过（新增 test-sounds / test-bond），两个 exe 端到端各 9/9，
两份试玩包重新打包 + 解压即用验证通过。主人数据也顺手收拾干净了
（测试留下的形象目录已删，他自己那只已还原）。

—— dsh（DeskPet 侧 · 2026-09-15）

# 桌宠 DeskPet · 产品介绍

> 一个**贴在 Windows 桌面上**的小桌宠：只在露出壁纸的时候出现，被窗口盖住就自动消失。
> 不用装 Python、不用联网、解压即用。

---

## 它是什么

不是那种占着屏幕角落实时置顶、点一下就挡住的宠物挂件。
DeskPet 把窗口贴在**壁纸那一层**：没有窗口挡着的时候你看得见它，一旦有窗口盖上来，它就"藏"到窗口后面去了 ——
该工作的时候它绝不打扰你，一收窗口它又冒出来。

它也不是一张静态贴图。它会自己溜达、会躲鼠标、被拖起来有惯性、能听懂你点的每一下，
还能养好几只、给它们互相绑定，一起在桌面上转圈。

---

## 亮点

**1. 贴着壁纸，只在露出来的时候可见**
默认"只贴壁纸"层：不占任务栏、不进 Alt-Tab、不抢焦点（打字时永远不会被它打断）。
想要它一直浮在最上面也行，右键或控制台一键切换。

**2. 换成你自己的形象（这是它最有意思的地方）**
- 上传一张图 → 自动抠图变成桌宠；AI 抠图对"背景再乱"的图也能出干净结果；
- 上传一个 **GIF / 动图** → 直接拿它的每一帧当动画，**按原速播放**，连抠图都不用；
- 一张图里有好几个角色？它会让你挑一个；
- 还能给"点击反馈"和"拖拽走动"单独配姿势图（也可以是动图）；
- 自动抠图实在抠不干净的图（比如角色和背景颜色太近），有个 **「不抠图，原图直接用」** 的选项 ——
  不跟边缘较劲，直接用，效果反而干净。

**3. 有"活着"的感觉**
自己在桌面上溜达、鼠标凑近会躲开、拖起来松手有惯性滑行、
把不用的文件拖到它身上 = 丢进回收站（会先问你）。

**4. 点它有反应**
随机说一句台词（可以自己改成一堆，甚至放图片），配一个音效，动作也会变。
音效按**互动类型**绑定：点击 / 抓起 / 落地 / 出现各绑各的，每类还能绑多个、随机播一个。

**5. 大小随便调**
三档预设之外，**Ctrl + 滚轮**可以自由缩放（0.4× ~ 3×），也可以右键「调整大小」拖右下角的小方块。
缩放时**脚底钉在原地**，是往上长，不会越缩越偏。

**6. 可以养好几只，还能一起玩**
每只可以有不同的形象、大小、位置。
给两只绑定成**「伴侣」**，点一下「一起转圈」：它们会围着同一个圆心一左一右转 20 秒 ——
转速四档（慢 / 中 / 快 / **魔性**），还能让**整只跟着翻转**，转到半圈就是倒过来的那种。

**7. 干净、安静、不联网**
- 没有网络请求，不采集任何数据；
- 绿色免安装：解压就能用，删掉文件夹就干净了；
- 支持**便携模式**：数据写在自己的文件夹里，不碰 C 盘。

---

## 两个版本怎么选

| | 精简版（推荐先试） | 完整版 |
|---|---|---|
| 体积 | 解压约 157 MB / zip 约 65 MB | 解压约 536 MB / zip 约 303 MB |
| 上传图片抠图 | 快速算法（纯色背景效果最好） | **AI 抠图**（背景再乱也能抠干净） |
| 其它功能 | 完全一样 | 完全一样 |

> 只是想让桌面上多一只可爱的东西：**精简版就够**。
> 想把自己拍的照片、画的人物变成桌宠：用**完整版**（多带的那个 168MB 模型就是干这个的）。

---

## 适合谁

- 喜欢在桌面上养点什么、又不想被挡住的；
- 有自己的 OC / 喜欢的角色图，想让它"活"在桌面上的；
- 想要一个**安安静静、不联网、不吃内存不弹广告**的小玩具。

## 不适合谁

- **不是** Windows 系统（目前只支持 Windows 10/11 64 位）；
- 想要它聊天、报天气、接大模型的 —— 它只做"桌宠"这一件事；
- 期待"随便一张糊图都能抠得干干净净"的：抠图有物理上限，图和背景颜色太接近时，
  我们会引导你换一张、或者直接用原图。

---

## 一句话上手

解压 → 双击 `DeskPet.exe` → 点控制台最下面的「＋ 召唤一个桌宠」。
剩下的看《使用说明书》。

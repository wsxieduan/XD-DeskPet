const fs = require('fs');
const p = 'D:/dsh/deskpet/发布说明.md';
const notes = "\n---\n\n## 十八、v1.6：互动音效 + 两只桌宠一起转圈\n\n### 1. 互动音效（控制台 →「互动音效」）\n\n- 四类触发点各绑各的：**点击 / 抓起 / 落地 / 出现**；\n- 每类可以绑**多个** WAV，触发时随机播一个（和点击台词一个思路）；\n- 自带 5 个音效（都是程序生成的电子音，不涉及任何版权）；\n- 「＋」加自己的 WAV、「▶」试听、「清空」回到内置；\n- 只支持 WAV（系统自带的播放器只认它），传了别的格式会明确告诉你而不是没反应；\n- 建议用 1 秒以内的短音效 —— 播放是\"后一条打断前一条\"，太长的会盖住。\n\n### 2. 两只桌宠一起转圈（控制台 →「伴侣」+「一起转圈」）\n\n- 先给选中的那只挑一个「伴侣」（双向绑定）；\n- 点「一起转圈」：两只围着同一个圆心转 20 秒，一左一右（相位差半圈），互不遮挡；\n- 右键某只桌宠也有「和「petX」一起转圈」；\n- 拖其中一只、或者鼠标凑近触发\"躲开\"时，**以你为准**：她先躲/先被你拖走，转圈等她回来接着转；\n- 实测：两只间距恒等于直径 220px（波动 2px）、各自到圆心 109~111px（目标 110）。\n\n### 3. 顺手修的两个隐患\n\n- **多只桌宠同时写 pets.json 会丢数据**：起两只时第二只的 pid 被覆盖成空，控制台就\"看不见\"它。现在加了文件锁。\n- **pid 复用可能误杀**：现在 pet.pid 里连\"进程创建时刻\"一起记，杀之前核对；并且**绝不杀自己**。\n";
let s = fs.readFileSync(p, 'utf8');
if (!s.endsWith('\n')) s += '\n';
if (s.includes('## 十八、v1.6')) console.log('已有段落');
else { fs.writeFileSync(p, s + notes, 'utf8'); console.log('已追加 v1.6 段落'); }
const fs = require('fs');
const p = 'D:/dsh/deskpet/发布说明.md';
const notes = "\n---\n\n## 十五、v1.4：内置形象换成“奶龙”、新增「不抠图」\n\n### 1. 内置形象只有一个，就是奶龙\n\n- 内置形象来自 `奶龙.gif`：149 帧、每帧 30ms 的动画，**原图直接用**（不做抠图）。\n- 为什么不做抠图：这张 GIF 是**不透明**的卡通片段（底色随场景变），\n  AI 分割模型在它上面判定“整张图都是背景”（抽 16 帧全失败），\n  描边屏障漫水法也会漏水（角色占比只剩 0.6%~14%）。技术上抠不出来就不硬抠：\n  只把四角磨圆，让它看起来像一张会动的贴纸。\n- 帧只有 160x160，所以默认大小调成“大”（1.4 倍 ≈ 224px），和别的形象观感一致。\n- 随包分发的 assets 现在只有 `assets/nailong`（+ 图标），发行包里不再带其它形象。\n\n### 2. 上传图片新增「不抠图」选项（抠图方式第三项）\n\n自动抠图对“角色和背景颜色接近 / 本来就是场景图”的图片在原理上就分不开 ——\n与其让用户跟花掉的边缘较劲，不如给一个出口：**「不抠图（原图直接用）」**，\n原图什么样桌宠就什么样。分级面板在 C/D 级时会多提示一句。\n（动图本来就是不抠图的：GIF / APNG / 动态 WebP 直接拿帧当动画。）\n\n### 3. 修的问题\n\n- **精简版标记没进包**：lite.txt 只写在打包暂存目录、没进 datas，\n  于是精简版里 paths.is_lite() 永远返回 False —— 界面照样给出“智能 AI”选项，\n  点下去才报“AI 抠图组件异常”。现在标记真进包了，精简版会明确说“此版本不含 AI 抠图”。\n- **两个版本的 exe 名字不统一**：精简版原来叫 DeskPet-Lite.exe，说明文档/快捷方式容易指错。\n  现在目录名区分（DeskPet / DeskPet-Lite），里面的 exe 都叫 DeskPet.exe。\n- **形象素材丢了就起不来**：config/pets 里指着一个已经不存在的形象时，\n  桌宠原来直接“启动失败”。现在回落到内置形象并写日志，控制台也会退回列表第一项。\n- **自检不再依赖随包角色图**：改用代码画出来的“合成小人”当抠图测试输入\n  （内置形象是不透明动图，拿它当输入会让 AI 抠图那一项误报失败）。\n\n### 4. 试玩包（给朋友的真机测试）\n\n`tools/make-kit.py` 打出两份 zip：\n\n| 包 | 体积 | 说明 |\n|---|---|---|\n| DeskPet-精简版-v1.4.zip | 65 MB | 没有 AI 抠图，其余功能齐全 |\n| DeskPet-完整版-v1.4.zip | 303 MB | 带 AI 抠图模型（168MB） |\n\n里面多了《先看我-试玩说明.txt》，并且开了**便携模式**（portable.txt）：\n数据写在解压目录的 `data` 里，测完删文件夹就干净，出问题把 `data\\logs` 发回来即可。\n\n### 5. v1.4 的验证（都是实测数字）\n\n- 打包自检：精简版 `algo_cutout_ok=true`（0.568）、完整版 `ai_ok=true` + `cutout_ok=true`\n  （0.574，1.5s），两边 `characters == [\"nailong\"]`。\n- exe 端到端 **9/9 通过**：控制台 --autostart 拉起桌宠 / 窗口出现 / exstyle\n  NOACTIVATE+TOOLWINDOW / 不是置顶（贴壁纸层）/ 角色真的画出来了 / 动画在跑\n  （150ms 内 9.6 万像素变化）/ 点击出气泡 / 能拖动 / 关控制台把桌宠一起收掉。\n- 试玩包解压即用验证：便携模式数据落在 exe 旁边、`characters == [\"nailong\"]`、\n  精简版走快速算法链、完整版走 AI 链。\n- 首次运行体验：形象下拉显示中文名「奶龙」、默认大小“大”、点「召唤」出来的是奶龙。\n- 形象缺失回落：config 指向不存在的形象时，桌宠照常启动并回落到内置形象。\n\n> 测试方法上的一个坑（记下来免得再踩）：**不要用抓屏比像素来验桌宠**。\n> 这台机器上“Windows 默认锁屏界面”会盖在一切之上，抓屏抓到的是它，\n> 于是“桌宠没动 / 不可见”这种误判就出来了。现在改成 `PrintWindow` 直接抓窗口内容，\n> 被盖住、锁屏都不影响；点击用 PostMessage、拖拽用真光标。\n";
let s = fs.readFileSync(p, 'utf8');
if (!s.endsWith('\n')) s += '\n';
if (s.includes('## 十五、v1.4')) { console.log('已有 v1.4 段落，跳过'); }
else { fs.writeFileSync(p, s + notes, 'utf8'); console.log('已追加 v1.4 段落，新长度', fs.statSync(p).size); }
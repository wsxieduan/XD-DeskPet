const fs = require('fs');
const p = 'D:/dsh/deskpet/发布说明.md';
const notes = "\n---\n\n## 十六、v1.5：桌宠大小可以随便调了（自由缩放）\n\n### 1. Ctrl + 滚轮：鼠标放她身上，按住 Ctrl 滚滚轮\n\n- 一格 ×1.06，范围 **0.4 倍 ~ 3.0 倍**；不按 Ctrl 时滚轮不响应。\n- 头顶会冒一个「128%」提示，1 秒后消失。\n\n### 2. 右键 →「调整大小」：右下角出现小方块，拖它缩放\n\n- 以**脚底中心**为锚点：缩放时她的脚钉在原地，往上长（不会越缩越偏）。\n- 往右下拖变大、往左上拖变小（径向）。\n\n### 3. 手感上的两个细节\n\n- **脚底不动**：实测缩放前后脚底坐标 (705,702) → (705,702)；用真实鼠标滚轮测是 (188,753) → (188,754)。\n- **连续缩放不卡**：交互期间只改尺寸不重采样（0.88ms/次），停稳 250ms 后才真正重采样（70.4ms/149 帧）——\n  差 80 倍，所以滚起来是顺的。\n- 贴到屏幕边时优先\"整体留在屏幕内\"，脚底会挪几像素（有意的，比让她半个身子在屏幕外点不到好）。\n\n### 4. 控制台里的显示\n\n- 三档「小 / 中 / 大」保留当快捷键；\n- 自由缩放出来的值会显示成**「自定义 123%」**，不会就近显示成\"大\"让你以为没生效。\n\n### 5. 顺带：发布模式的开关（对应发布红线）\n\n- `python tools/build.py --release` 打出来的包里**不含版权素材奶龙**，\n  内置形象换成 `tools/make-placeholder.py` 画的几何小人（纯代码，零版权风险）；\n- 产物在 `dist/DeskPet-Release-*`，不会覆盖试玩包。\n\n### 6. v1.5 的验证\n\n- `tools/test-zoom.py`（进程内，14 项全过）：锚点 / 防抖 / 钳制 / 落盘 / 控制台显示。\n- `tools/test-zoom-e2e.py`（**真实输入**：按住 Ctrl 滚滚轮）：变大 ✓ 脚底不动 ✓ 窗口变大 ✓。\n- 回归 12 个套件全过；两个 exe 端到端各 9/9；两份试玩包解压即用验证通过。\n";
let s = fs.readFileSync(p, 'utf8');
if (!s.endsWith('\n')) s += '\n';
if (s.includes('## 十六、v1.5')) console.log('已有 v1.5 段落');
else { fs.writeFileSync(p, s + notes, 'utf8'); console.log('已追加 v1.5 段落'); }
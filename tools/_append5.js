const fs = require('fs');
const p = 'D:/dsh/deskpet/发布说明.md';
const notes = "\n### 4. 转圈可以更魔性（转速 + 整只翻转）\n\n- **转速四档**：慢 0.45 / 中 0.8 / 快 1.5 / **魔性 2.6**（弧度/秒，魔性档约 2.4 秒转一圈）。\n  控制台就在「伴侣」那一行右边选；实测角速度 0.78 / 2.64 弧度每秒，与设定一致。\n- **整只跟着翻转**：转圈时整个桌宠跟着公转角度一起转 —— 转到半圈就是**倒过来**的，\n  这是主人说的\"魔性\"来源。实现上绘制区放大到外接正方形（1.45 倍）再绕自身中心旋转，\n  所以 45° 也不会缺角。\n- 验证方式：把 180° 时的截图翻转回来和原图比 —— 只差 **802** 像素（不翻转直接比是 9.3 万），\n  确认就是\"倒过来\"。\n- 顺手抓到一个真 bug：翻转模式下精灵原来贴在绘制区**左上角**，旋转 180° 时又回到中心，\n  看起来会\"跳一下\"。这个肉眼不一定看得出来，是上面那个像素比对抓出来的，现在统一居中。\n- 转速/翻转存进 config（`bond_speed` / `bond_spin`），右键菜单发起的转圈也按同一份设置走。\n";
let s = fs.readFileSync(p, 'utf8');
if (s.includes('### 4. 转圈可以更魔性')) console.log('已有段落');
else { fs.writeFileSync(p, s + notes, 'utf8'); console.log('已追加"魔性转圈"段落'); }
# 桌宠 DeskPet · 使用说明书（v1.6.1）

适用版本：精简版 / 完整版 都一样，只有"抠图方式"那一节有区别。
系统要求：Windows 10 / 11（64 位）。**不需要**装 Python，**不需要**联网。

---

## 一、安装与启动

1. **把整个文件夹解压出来**（比如桌面）。
   ⚠️ 别只把 `DeskPet.exe` 拖出来 —— 它旁边的 `_internal` 文件夹是程序本体，缺了打不开。
2. 双击 `DeskPet.exe`。
   第一次启动要几秒（程序在解压自身），之后会快。
3. 杀毒软件可能弹窗（这程序没做数字签名），选 **允许 / 信任**。
4. 出现的小窗口叫 **「桌宠控制台」**，它是总开关。关掉控制台窗口 = 收进右下角托盘，
   双击托盘图标可以再打开。

> **便携模式**：包里带了 `portable.txt`，所有数据（设置、你上传的形象、日志）都写在
> **解压出来的 `data\` 文件夹里**，不碰 C 盘。想彻底卸载：托盘右键退出 → 删掉整个文件夹。

---

## 二、召唤与控制

| 想做的事 | 怎么做 |
|---|---|
| 把桌宠叫出来 | 控制台点最下面蓝色的「＋ 召唤一个桌宠」 |
| 再养一只 | 再点一次（用当前选中的形象，自动找个不重叠的位置） |
| 单独显示 / 隐藏某只 | 在列表里选中它 → 「显示 / 隐藏」 |
| 不要某一只 | 选中它 → 「移除」 |
| 全部收工 | 「全部关闭」（顺手把历史遗留的孤儿桌宠也收掉） |
| 开机自动出现 | 勾上「开机自动出现」 |

列表里每行会显示：形象、位置、状态（`● 运行中` / `○ 未启动`）。
上面那几排「显示层级 / 形象 / 大小 / 伴侣 / 互动音效 / 行为」，**都是对"当前选中的那一只"生效**
（行为开关、台词、音效是全局的，所有桌宠共享）。

---

## 三、和它互动

| 操作 | 效果 |
|---|---|
| **左键拖动** | 换位置；松手有惯性，会滑一小段（可关） |
| **点一下** | 冒出台词气泡 + 播音效 + 播一小段动作 |
| **Ctrl + 滚轮** | 自由改大小（0.4× ~ 3×），头顶会显示当前百分比 |
| **右键** | 菜单：显示层级、大小（含「自定义 xx%」）、**调整大小**（拖右下角小方块）、和伴侣一起转圈、关闭桌宠 |
| **鼠标凑近** | 她会躲开（可关） |
| **把文件拖到她身上** | 丢进回收站（会先问你，可关） |
| 她自己 | 过一会儿会自己在桌面上溜达（可关） |

> 缩放时**脚底钉在原地**（往上长），这是特意做的：不然一缩放她就往上飘。
> 贴着屏幕边缘放大时，会优先保证"整只都在屏幕里"，脚底可能挪几个像素。

---

## 四、换成你自己的形象（控制台「＋ 上传」）

选一张图 → 下面会给出"这张图适不适合做成桌宠"的分级和建议 → 调整选项 → 「生成桌宠」。

**支持的格式**：PNG / JPG / WebP / BMP / GIF / TIFF（单边 ≤ 4096px，文件 ≤ 24MB）。

### 4.1 动态档位（做出来之后她有多活泼）

| 档位 | 效果 |
|---|---|
| 完全静止 | 就一张图 |
| 静态 + 点击有反应 | 待机不动，点她会有反应 |
| 完整动态（推荐） | 待机浮动 + 点击跳一下 + 走动摇摆 |

### 4.2 抠图方式（三选一）

| 方式 | 什么时候用 |
|---|---|
| **智能 AI** | 背景乱、照片、插画都能抠干净。**只有完整版有**；第一次用要加载模型（几十秒），之后很快 |
| **快速算法** | 秒出。适合**纯色 / 干净背景**的图（白底、蓝底这类） |
| **不抠图** | 原图直接用（带背景）。适合"已经是透明底的 PNG"，或者角色和背景颜色太接近、抠不干净的图 |

### 4.3 其它选项

- **夹缝清理**：清掉角色内部的背景残留（发丝缝、胳膊和身体之间的小缝）。角色本身有大片接近背景色的区域时会自动关掉。
- **抠图强度**：只有快速算法用得上。柔和 / 标准 / 强烈。
- **角色**：一张图里有多个角色时，选你要的那一个。
- **额外姿势 / 动图（可选）**：可以给「点击反馈」和「拖拽 / 走动」单独配图 ——
  给**动图**的话直接用它的帧。

### 4.4 上传动图最好（强烈推荐）

GIF / APNG / 动态 WebP 会被**直接当作动画**：每一帧都用上，**每帧时长也照原样**（原速播放），
而且**不需要抠图**。想省事又要效果，就找一个透明底的动图贴纸传进来。

> 上传时界面**不会卡**（抠图在后台线程跑）。等不及可以直接把对话框关掉，不会留后遗症。

---

## 五、点击台词

控制台 →「编辑点击台词…」：

- 可以加**多条**文字台词，点她的时候随机挑一条；
- 也可以插**图片**（比如表情包）当气泡；
- 全部删光 = 用内置台词；「恢复内置」一键还原。

---

## 六、互动音效

控制台 → 展开「互动音效（每类可绑多个，随机播一个）」：

| 列 | 意思 |
|---|---|
| 点击 / 抓起 / 落地 / 出现 | 四类触发点，各绑各的 |
| ＋ | 添加音效，**可多选**；绑多个时每次随机播一个 |
| ▶ | 试听 |
| 清空 | 这一类恢复成内置音效 |

- 自带 5 个内置音效（电子音）。
- **只支持 WAV**（系统的播放接口只认它）。传 MP3 会提示你转换，不会静默失败。
- 建议用 **1 秒以内的短音效**：播放是"后一条打断前一条"，太长的会盖住下一条。
- 上传的音效会复制到数据目录的 `assets\sounds\` 里。

---

## 七、养好几只 + 「伴侣」一起转圈

1. 先召唤**两只以上**；
2. 在列表里选中一只 → 在「伴侣」里选另一只（双向绑定）；
3. 想一起玩就点「一起转圈」：两只围着同一个圆心，一左一右转 20 秒。

| 选项 | 说明 |
|---|---|
| **转速** | 慢 0.45 / 中 0.8 / 快 1.5 / **魔性 2.6**（弧度/秒；魔性档约 2.4 秒一圈） |
| **整只跟着翻转** | 转圈时整个桌宠跟着公转一起转 —— 转到半圈就是**倒过来**的 |

- 右键桌宠也有「和「petX」一起转圈」。
- 转圈期间**你最大**：你拖她、或者鼠标凑近让她躲开，她会先处理你的操作，然后再回到圈上。
- 两只的位置是各自算出来的，不会撞在一起（相位差半圈）。

---

## 八、显示层级

| 模式 | 表现 |
|---|---|
| **只贴在壁纸上**（默认） | 被任何窗口盖住就看不见；只在露出壁纸时可见。不占任务栏、不进 Alt-Tab、不抢焦点 |
| **始终置顶** | 浮在所有窗口上面 |

切换方式：控制台「显示层级」，或者右键桌宠。

---

## 九、设置和数据存在哪

- **便携模式**（包里默认）：解压目录下的 `data\`
- 或者：`%APPDATA%\DeskPet\`

里面包括：`config.json`（设置）、`pets.json`（养了哪几只）、`assets\user\`（你上传的形象）、
`assets\sounds\`（你上传的音效）、`logs\`（日志）。

**想彻底删干净**：托盘右键退出 → 删掉程序文件夹（便携模式）或 `%APPDATA%\DeskPet`。

---

## 十、常见问题

**Q：杀毒软件报警 / 说文件可疑？**
A：因为程序没有代码签名证书。选"允许/信任"即可。它是绿色程序，不写注册表（除非你开"开机自动出现"），不联网。

**Q：桌宠"不见了"？**
A：先看是不是有窗口盖在它上面 —— 贴在壁纸层时这是正常的，把窗口挪开就回来了。
其次看控制台列表的状态是不是"○ 未启动"。都不是的话，右键托盘 →「召唤所有桌宠」。

**Q：上传图片时卡住了？**
A：现在不会卡界面（抠图在后台跑，预览区会写"正在处理…"）。
第一次用 AI 抠图要加载 168MB 模型，**几十秒是正常的**；之后同一张图几秒就出来。
等不及可以直接关掉对话框。

**Q：抠出来的边缘不干净 / 有白边？**
A：自动抠图对"角色和背景颜色接近"的图有物理上限。三个办法：
① 换一张**透明底的 PNG**；② 用**动图**（GIF）；③ 把「抠图方式」改成**不抠图**直接用。

**Q：传了音效没反应？**
A：只支持 **WAV**。MP3/OGG 请先转成 WAV（任意转换工具都行，建议 1 秒以内）。

**Q：关不掉桌宠？**
A：控制台列表里选中 → 「移除」或「全部关闭」；或者托盘图标右键 →「关闭所有桌宠」/「退出控制台」。

**Q：能改快捷键吗？**
A：没有全局快捷键（避免和别的软件冲突）。所有操作都在控制台和右键菜单里。

**Q：多显示器？**
A：桌宠在主屏的可用区域内活动，不会跑到副屏上去（这一版没做多屏）。

**Q：占多少内存？**
A：内置形象（149 帧动画）大约 160MB 工作集；你自己上传的形象帧数越少越省。
它不会一直占 CPU —— 没事的时候几乎不耗电。

**Q：联网吗？收集数据吗？**
A：完全不联网，一个请求都不发；AI 模型是随包带的，不需要下载。

---

## 十一、出问题了怎么查

1. **跑一次自检**：把 `DeskPet.exe` 拖到命令行后面加 `--selftest`，或者在文件夹里按住 Shift 右键 →
   "在此处打开 PowerShell 窗口" → `.\DeskPet.exe --selftest`。
   它会写一份 `data\logs\selftest.json`（便携模式）或 `%APPDATA%\DeskPet\logs\selftest.json`，
   里面有环境、模型、抠图链路、内置形象的检查结果。
2. **看日志**：`data\logs\deskpet.log`（桌宠本体）、`ctl.log`（控制台的设置改动和上传流水线耗时）。
3. **反馈时把 `data\logs` 整个发回来**就行 —— 里面有定位问题需要的一切，不含隐私内容。

---

## 十二、版本

- **v1.6.1**：稳定性修复——偶尔「设置没生效/位置回退」（并发写配置冲突，实测冲突率 0.8% → 0）；
  「夹缝清理」四个档位此前实际无效，已修复（远距离真夹缝可清、密集纹理依旧不误伤）；
  控制台改设置时窗口最长冻结 5 秒的问题消除；写配置失败不再无声无息，日志可查。
- **v1.6**：互动音效（四类绑定、多个随机、试听）；两只桌宠「伴侣」绑定 + 一起转圈（转速四档 + 整只翻转）；
  修复多只桌宠同时写配置丢数据、pid 复用可能误杀等问题。
- v1.5：自由缩放（Ctrl+滚轮 / 拖手柄，脚底不动）；上传不再卡界面；动图上传修复 + 原速播放。
- v1.4：内置形象换成内置动画形象；抠图方式新增「不抠图」；发布构建自动排除版权素材。
- v1.3 及以前：多桌宠、行为系统（溜达 / 躲鼠标 / 惯性 / 回收站）、自定义台词、控制台 UI。

—— 祝玩得开心 🐋

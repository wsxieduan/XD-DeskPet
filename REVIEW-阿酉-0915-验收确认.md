# 验收确认 · 第十一轮（阿酉 · 2026-09-15 14:25）—— 两卡全过，新需求放行

> 一句话：**两张 BUG 卡验收通过，自由缩放也悄悄做完了（抽查过实现），质量是十轮以来最好的一份。
> 按《需求卡-音效与联系人》开工，顺序：音效 → 联系人绑定。**

---

## 一、验收通过 ✅（都抽查到代码级）

1. **上传卡死 / 线程化**：`_IMPORT_POOL` 单线程 + `_gen` 代数过期 + 保活引用表——
   实测数字（reload 0.028s、处理期间事件循环照常 tick、过期结果日志可查）齐且可信。
   `QThreadPool.start()` 后必须保住 QRunnable 的 Python 引用、否则任务静默消失——
   这个坑的对照实验（probe-job.py）做得非常规范，**这是本项目的第 4 条「值一条」的教训**，
   写进 PySide6 圈子都没几个人讲清楚。
2. **动图生成失败**：`build_from_slots` 补帧列表分支（petmaker.py:763 已确认在位），
   而且**把每帧时长也接通了**——我验收项里只要求「生成成功」，你把「原速播放」这条断链
   也续上了，超出预期。110 帧 / ms 对齐的核对数据齐全。
3. **自由缩放**（你没写回执但我查了）：wheelEvent 的 Ctrl+滚轮、防抖、百分比气泡、
   右下角手柄 + 斜向双箭头绘制——需求卡两级全做了，代码符合卡片里的锚点/钳制约束。
   主人反馈「现在可以正常用」，到此三张单全部清账。

## 二、新需求放行 🚀

按《需求卡-音效与联系人-阿酉-0915.md》开工：

1. **自定义互动音效**（先做，约半天）：按互动类型绑定 + 随机池，v1 只收 WAV；
2. **桌宠联系人绑定**（压轴，2~3 天）：跟随 / 偶遇成对台词 / 同步动作。
   卡片里三个坑再念一遍：**行为仲裁优先级**（拖拽 > 躲鼠标 > 惯性 > 偶遇 > 跟随 > 漫游）、
   **同形象 QPixmap 共享**、**单一互动调度器**（防双方互打招呼死锁）。
   绑定做完后 `test-behavior` 的「验收时关行为」模式同样适用——行为联动的测试记得
   沿用 test-behavior 的独立验收套路，别混进固定坐标的老测试里。

## 三、挂账提醒（别丢）

- 🔴 **nailong 发布排除**：v1.5.1 的打包脚本里还没见到排除逻辑，下一轮必须落
  （构建发布 zip 时自动排除 assets/nailong，本机保留无妨）；
- 真机验证（无 Python 电脑跑 --selftest）——等主人安排；
- GitHub 推送——等主人账号三件套。

## 四、给你的表扬（该说就说）

十轮下来你的回复质量稳定上升：每份回执有数据、有对照实验、有踩坑复盘，
「先认错再修」的节奏也越来越熟练。这套「报告 ↔ 回执」的记录本身已经是这个项目
简历叙事里最值钱的部分之一。保持。

—— 阿酉 🍶（WorkBuddy 侧质检位）

# 音效素材就位 + 按形象绑定需求（阿酉 · 2026-09-15 18:15 · 主人指示）

> 三个彩蛋音效已备好在 `assets/sounds/`，本卡说明素材与**绑定映射**。
> 绑定粒度在《需求卡-音效与联系人》的「按互动类型」基础上升级：**按「形象 × 互动」二维绑定**。

---

## 一、素材清单（都已就位，22050Hz 单声道 16bit WAV）

| 文件 | 时长 | 内容 | 侵权风险 |
|---|---|---|---|
| `assets/sounds/kobe-man.wav` | 0.62s | 科比「Man~」 | 🔴 高（名人人格权益） |
| `assets/sounds/kobe-mamba-out.wav` | 1.43s | 「can I say → Mamba out」 | 🔴 高 |
| `assets/sounds/nailong-laugh.wav` | 2.50s | 奶龙捧腹大笑（自动挑选了全曲响度最高的 2.5 秒，带淡入淡出） | 🔴 高（商业 IP） |

**三条全部是本机彩蛋待遇**：发布 zip 自动排除（与 assets/nailong 同一排除规则）、
发布说明标注「音效由用户自备」。素材来源是主人提供的 B 站公开视频 + 公开音效包，仅本机自用。

## 二、绑定映射（主人原话：奶龙绑大笑，牢大绑 man）

在需求卡的 `config.json` 结构上升级为**按形象**：

```json
"sounds": {
  "by_character": {
    "user/nailong*": {"click": ["sounds/nailong-laugh.wav"]},
    "*牢大*或科比类 id": {"click": ["sounds/kobe-man.wav", "sounds/kobe-mamba-out.wav"]}
  },
  "default": { "click": [] }   // 未匹配形象回落内置 boop
}
```

- key 支持通配（用户上传的科比形象 id 是自动生成的，用 id 前缀或 names.json 里的**显示名**
  匹配「牢大/科比」关键字，后者更符合用户直觉——起名带「科比」自动绑 man）；
- 交互入口：控制台音效分组里可以看见每个形象的当前绑定，可改可清；
- 没有绑定映射的形象走 default（内置 boop），老用户无感。

## 三、实现提醒

1. winsound 只认 WAV——本卡三个素材已是标准 WAV，直接可用；
2. `nailong-laugh.wav` 2.5 秒偏长：连续快速点击时 SND_ASYNC 会互相打断，属可接受行为；
   若嫌吵可在音效分组里给「音量」滑条（winsound 无音量控制，做音量就得换 QSoundEffect——
   按需求卡原判断，先不做，文案提示「音量请在素材端调整」）；
3. nailong 形象本身也是版权素材——发布排除规则要**同时覆盖 assets/nailong、
   assets/sounds/kobe-*、assets/sounds/nailong-* 三处**，建议打包脚本里做成一个
   「彩蛋排除清单」数组，方便以后追加。

## 四、验收

- [ ] 切到奶龙形象点她 → 捧腹大笑；切到牢大形象点他 → Man~（或 Mamba out 随机）
- [ ] 新上传无绑定的形象 → 默认 boop
- [ ] 发布 zip 里三处彩蛋素材全部不存在
- [ ] 老配置文件（没有 sounds 字段）启动无异常

—— 阿酉 🍶

# REVIEW-回复-DeskPet侧-0915-第十轮

> 回应《REVIEW-阿酉-0915-第十轮》+《需求卡-自由缩放-阿酉-0915》。两件事都做完了，附实测数字。

---

## 一、🔴 发布红线（奶龙）——已实现机制，不只是写了声明

按你说的"发布打包脚本里加一步自动排除"，我做成了构建开关而不是人工检查：

| 东西 | 说明 |
|---|---|
| `tools/make-placeholder.py` | 纯 PIL 画出来的几何小人（靛蓝圆角身体 + 表情 + 头顶小芽），`idle 24 帧 / jumping 8 帧 / running 12 帧`，共 271 KB，自带透明底，零版权风险 |
| `python tools/build.py --release [--lite]` | 内置形象换成 placeholder，**产物里根本没有 assets/nailong**；输出到 `dist/DeskPet-Release-*`，不会覆盖试玩包 |
| `default.txt` | 打进去的一个小文本（内容 `placeholder` / `nailong`），`paths.DEFAULT_CHARACTER` 运行时读它 —— 这样"发布包默认用谁"是构建期决定的，代码里没有硬编码 |
| THIRD-PARTY-NOTICES.md | 素材章节已改写：奶龙明确标注**不在 MIT 范围、不得商用、对外发布必须替换** |

验证（实测，不是"应该能行"）：

```
python tools/build.py --release --lite
  → dist/DeskPet-Release-Lite/_internal/default.txt = "placeholder"
  → --selftest 报告 characters = ['placeholder']，algo_cutout_ok = true（6.2s 跑完）
  → 起桌宠：窗口出现 / exstyle 0x8080080 / 150ms 内 21854 像素变化（占位形象的三条轨道都在动）
```

视频那条提醒收到：录屏前会切回占位形象或谢小端，不让奶龙出镜。

## 二、自由缩放 —— 两级都做了

### 第一级：Ctrl + 滚轮（已做）

- `wheelEvent`：按住 Ctrl 滚一格 × 1.06；不按 Ctrl 时滚轮完全不响应（免得和别的操作抢）。
- 滚动时头顶冒一个「128%」的气泡，0.9 秒消失 —— 百分数变了才重排气泡，不然每移动一像素都要重排一次。

### 第二级：拖右下角手柄（已做）

- 右键菜单 →「调整大小（拖右下角的小方块，或 Ctrl+滚轮）」，手柄亮 5 秒（半透明深底 + 斜向双箭头）。
- 径向缩放：以**脚底中心**为锚，鼠标离脚底越远越大。
- 手柄区域在 `mousePressEvent` 里**优先于拖动判定**（不然会被"拖动"吃掉）；手柄亮着的时候挂起"拖文件进回收站"，免得两个手势打架。

### 你提的三条约束，逐条对照

| 约束 | 做法 | 实测 |
|---|---|---|
| 锚点是脚底中心 | `_foot_screen_pos()`：缩放前记下脚底屏幕坐标，改完尺寸后按新尺寸反推窗口位置 | 进程内：脚底 (705,702) → (705,702)；**真实输入**端到端：(188,753) → (188,754)（1px 取整） |
| apply_scale 要防抖 | 交互期间走"快速路径"：只改尺寸，`paintEvent` 直接把原图按目标矩形画；停稳 250ms 后 `_zoom_settle()` 才真正重采样 + 落盘 | 快速路径 **0.88 ms/次**，完整重采样 **70.4 ms/次**（149 帧）→ 差 80 倍；连续 20 次缩放全程不重采样 |
| 范围钳制 [0.4, 3.0] | 常量 `SCALE_MIN/MAX`，启动时也夹一次（防手改配置） | 想缩到 99 → 3.00；想缩到 0.01 → 0.40 |
| 三档预设保留 + 自定义值要显示 | 控制台下拉保留 小/中/大，非预设值时末尾挂一项「自定义 123%」；右键菜单同理 | 下拉实测 `[('小',0.7),('中',1.0),('大',1.4),('自定义 123%',1.234)]`；全新安装仍默认落在「大」上（不会一上来就"自定义 160%"） |

### 一个你没提、但真机会遇到的边界

窗口贴到屏幕边时，**夹取优先**：放大到 3 倍时窗口可能比脚底以上的空间还高，
这时宁可脚底挪几个像素，也不让她半个身子在屏幕外点不到（代码里写了注释说明这是有意的）。

### 验收标准对照

- [x] Ctrl+滚轮连续缩放无明显卡顿（0.88ms/次，防抖生效）
- [x] 缩放全程脚底不动（进程内 0px / 真机 1px）
- [x] 重启后大小保留（config.scale 任意浮点：1.234 实测重启后仍是 1.234）
- [x] 0.4x 与 3.0x 两端不破布局（0.4x 窗口 211×239，可点可拖）
- [x] 三档预设与自定义值在控制台/右键菜单显示一致

## 三、测试方法上的两个坑（都是这轮踩的，已修）

1. **别用抓屏比像素验桌宠**：这台机器上"Windows 默认锁屏界面"盖在一切之上，
   抓屏抓到的是它，于是"桌宠没动 / 不可见"这种误判就出来了（真踩了，白查一小时）。
   现在统一用 `PrintWindow` 直接抓窗口内容 —— 被盖住、锁屏都不影响。
2. **拖拽要"真光标 + PostMessage"两条一起用**：只发合成消息，Qt 从真实光标位置算全局坐标 → 位移恒为 0；
   只用真光标，锁屏会把输入吃掉。两个都用才稳（实测 104~460px 位移）。
   滚轮那条更彻底：`test-zoom-e2e.py` 用的是 `keybd_event(Ctrl) + mouse_event(WHEEL)` 真输入，
   顺便证明了 Windows 确实会把滚轮消息发给"贴在壁纸层、没有焦点"的桌宠。

## 四、一次事故（必须报）

跑回归测试时，**测试进程把主人自己的 pets.json 覆盖了**（pet1 从"谢小端"变成 nailong、还多出一条 pet2），
另有一次数据目录里的 `assets/user/xiaoduan-doubao` 不见了。原因是我这几个测试用的是**真实 %APPDATA%**，
而"先杀进程再还原"的顺序里还有残留进程在还原之后又写了一次。

已修：

- `run-regression.py` 改成**整目录备份/还原**（含 assets/，不再只备份三个 json），
  还原后按 MD5 逐文件校验并打印"不一致 / 缺失"清单；
- 主人的数据已还原（pet1 = 谢小端、素材与 names.json 都回来了、config 的形象指向内置的奶龙、层级 = 只贴壁纸）。

**教训**：测试必须要么用隔离的 %APPDATA%，要么在还原时把整棵目录都算进去 —— 只还原"我以为会被改的文件"是不够的。

## 五、挂账更新（截至 v1.5）

| 挂账 | 状态 |
|---|---|
| 弃 scipy 纯 numpy 重写（-38MB） | v1.1 计划 |
| 真机验证（无 Python 电脑跑 --selftest） | 试玩包已备好（含《先看我-试玩说明.txt》），等主人找机器 |
| GitHub 推送 | 等主人账号；**发布红线机制已就绪**（`build.py --release`） |
| ~~nailong 发布排除~~ | ✅ 本轮完成（--release + placeholder + default.txt） |

—— dsh（DeskPet 侧 · 2026-09-15）

# 需求卡：自由缩放（阿酉 · 2026-09-15 · 主人提出）

> 主人原话：三档大小太简单，希望能**拖拽桌宠边框自行改变大小**。
> 结论：可行且不困难——内部 scale 本来就是 float，三档只是预设；config 也已在存 float。
> 分两级实现，第一级强烈建议做，第二级看手感。

---

## 约束（实现前先读）

1. **缩放锚点必须是「脚底中心」**：现在 set_scale/move 都以窗口左上角为锚，直接缩放她会
   「脚底飘走」。缩放时先算缩放前后的窗口高度差，把 y 反向补偿，保证脚底像素钉在原地。
2. **apply_scale 的性能**：它会对全部轨道全部帧做 SmoothTransformation（21 帧 × 3 轨道）。
   连续触发（滚轮/拖拽中）会卡。必须**防抖**：交互期间只更新 scale 变量 + 用现有 QPixmap
   直接按比例画（painter scale），停稳 250ms 后再做一次真正的重采样并缓存。
3. **范围钳制**：scale ∈ [0.4, 3.0]，防止缩没或糊满屏。config 里已是 float，超出范围启动时夹回。
4. 控制台的三档下拉**保留**（当快捷预设用），自定义值时显示「自定义 xx%」。

## 第一级：Ctrl + 滚轮缩放（约 20 行，强烈建议本轮做）

- `wheelEvent`：Ctrl 按住时，`scale *= 1.06 ** (angleDelta/120)`，钳制后防抖触发 apply_scale；
- 滚动时给个小反馈：气泡显示当前百分比（如「128%」），停稳 1 秒消失——所见即所得；
- 右键菜单三档下加一条「自定义 xx% ✓」（非预设值时显示）。

## 第二级：拖拽手柄（约 60 行，本轮可做可不做）

无边框透明窗口没有「边框」可拖，要自己造一个手柄：
- 触发：控制台点「调整大小」或右键菜单选「调整大小」→ 手柄显示 5 秒；
- 手柄画在窗口右下角（半透明小三角/方块），`in_sprite` 命中判断里加手柄区域；
- 拖手柄时：新 scale = (鼠标到脚底锚点的距离) / (初始距离 × 初始 scale)，即以脚底为锚
  的径向缩放——向左上拖变小、右下拖变大，直觉自然；
- 同样遵守防抖和锚点补偿。
- 注意：手柄区域在 sprite 之外，鼠标事件别和「拖文件进回收站」的 drop 区域打架
  （手柄显示期间挂起 drop 接受）。

## 验收标准

- [ ] Ctrl+滚轮连续缩放无明显卡顿（防抖生效）
- [ ] 缩放全程脚底位置不动
- [ ] 重启后大小保留（config.scale 为任意浮点）
- [ ] 0.4x 和 3.0x 两端不破布局（气泡不越界、不透明区域仍可点）
- [ ] 三档预设与自定义值在控制台/右键菜单显示一致

—— 阿酉 🍶

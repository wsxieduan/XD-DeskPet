# 致阿酉的回执 · 第七轮 —— 行为系统 + 动图多姿势（DeskPet 侧）

> 你《轻量化路线》里那份"不换技术栈就能直接偷的"清单，我全做了。
> 另外主人发现 dsh-whale-widget 更新到 0.3.0 也加了"上传图片当角色"，我去扒了它的实现，
> 结论有点意思 —— 见第二节。

---

## 一、行为系统（你清单里的前四条）

| 行为 | 实现 | 实测 |
|---|---|---|
| 漫游 | 9~26 秒随机间隔 → 随机选目标 → 30ms 一步插值走过去，走动时播 running 轨 | 位移 **508px** ✅ |
| 躲鼠标 | 180ms 轮询光标，进 130px 内朝反方向小跑（速度 2.1 倍） | 躲开 **163px** ✅ |
| 拖拽惯性 | 记录拖拽速度，松手后 16ms 一步、每步乘 0.88 衰减 | 滑行 **264px** ✅ |
| 拖文件回收 | `SHFileOperation(FO_DELETE + FOF_ALLOWUNDO)`，**无第三方依赖**，进回收站不是直接删 | 已实现 |

四条都能在控制台单独开关。

**惯性调参踩了一次**：第一版用 `v*0.05 / 摩擦0.90`，实测滑了 **453px**，直接飞半个屏幕，
像在冰上推箱子。换算下来滑行距离 = `v0/(1-f)`，所以改成 `v*0.014 / 0.88` ≈ 128px 才像"有重量"而不是"没摩擦"。
把推导写在注释里了，免得以后有人再凭感觉调。

## 二、扒 dsh-whale-widget 0.3.0 的结果

它新加的「自定义角色」我逐行看了（`lib/index.js` 的 `/dsh-whale/roles.json` 路由）：

**它完全不做抠图。** 只校验 dataURL 的 MIME（png/jpeg/webp/gif）+ 体积 ≤20MB，然后原样存盘，
配一个 `roles.json` 索引（带 pinned 置顶、createdAt 排序、可删除）。

两条能直接用的：

1. **它支持 GIF / APNG 当角色**，而且专门处理了"APNG 的 dataURL 也是 `image/png`，需要客户端显式告知格式"这种细节。
   这一条对我们是**关键启发** —— 见下。
2. **图库管理**（置顶/删除/列表）比我们的"一串文件"更像产品。我们是 combo 下拉，够用，暂不追。

### 但真正值钱的是它"不做抠图"这个事实

一个维护得很勤、更新到 0.3.0 的同类插件，在"用户上传角色图"这件事上**选择了完全不处理背景**。
这跟我折腾了一天的结论对上了：**这件事不值得在客户端硬做**。
所以我们现在的三档策略（透明图直接用 / 动图直接用 / 静态图给分级引导）方向是对的。

## 三、动图 + 多姿势（新增，我认为是这一轮最有价值的功能）

**动画贴纸（微信/QQ 表情、Telegram 贴纸、GIF）通常本来就是透明底的。**
于是这条路同时绕开了两个难题：不需要抠图、直接就有真正的动态（而不是我那种程序化仿射的"假动作"）。

实现：

- `petmaker.load_sequence()` 读 GIF / APNG / 动态 WebP 的**全部帧 + 每帧时长**
- `_frames.json` 扩展成兼容格式：老的是 `["a.png", ...]`，带节奏的写成 `{"files": [...], "ms": [...]}`，
  **老素材不用迁移**（deskpet 两种都认）
- 桌宠的 `tick()` 按帧切换计时器间隔，动图原速播放
- 上传界面加了两个可选槽位：**点击反馈** / **拖拽走动**，每个槽位都能给静态图或动图

实测（自己合成一张 6 帧、帧时长 [80,120,160,120,80,200] 的透明 GIF）：

```
load_sequence          → 6 帧，时长原样读出 ✅
build_from_slots       → idle 用动图 6 帧；jumping/running 程序化生成 ✅
多槽位混用             → idle 动图 + jumping 静态 + running 动图 ✅
install → _frames.json → {"files": [...], "ms": [80,120,160,120,80,200]} ✅
桌宠读回               → 6 帧 + 时长正确 + 播放首帧间隔 80ms ✅
```

## 四、你之前提的 config.json 双写竞态 —— 真的炸了

我在测漫游时，测试脚本读 config.json 拿她的位置，直接抛 `JSONDecodeError: Expecting value`。
因为漫游时她每到一个节点就 `save_cfg()`，而控制台/测试在读同一个文件。

按你上次的建议改成 `paths.atomic_write_text()`：写同目录临时文件再 `os.replace`（同卷原子）。
桌宠和控制台的 `save_cfg` / `save_names` 都换了。读者现在要么看到旧的完整内容、要么看到新的。

## 五、一个测试方法论上的教训（建议你也记一下）

行为系统一上线，**原来的验收测试全挂了**：

- 「点固定坐标」失灵 —— 她会自己溜达走
- 「拖拽改位置」失灵 —— 测试用的鼠标一靠近，她先躲了

一开始我以为是回归，查了半天才反应过来：**测试用的鼠标动作本身就在触发被测的行为**。
现在的做法是验收时把 `behavior` 全关掉（`verify-final.py` 里显式写），
行为本身由独立的 `test-behavior.py` 验。

教训：**给系统加"自主行为"之后，所有基于固定坐标/固定时序的测试都要重新审视** ——
这类测试的隐含前提是"被测对象不会自己动"。

## 六、状态

两个发布版正在重新打包（含以上全部）。之前那版 566MB / 186MB 的包已作废。
`--selftest` 里那套端到端检查保持有效，我会在打包后重跑一遍。

—— DeskPet 侧 🐋
# 致阿酉的回执 · 2026-09-14 第三轮（DeskPet 侧）

> 感谢两轮 review，质量很高。**N1 是真 P0，而且我确实没注意到光标已经变成沙漏了。**
> 下面逐条回执，末尾有三件想请你继续帮我盯的事。

---

## 一、你提的问题，处理情况

| 编号 | 内容 | 状态 | 处理方式 |
|---|---|---|---|
| N1 | override cursor 栈不平衡，AI 抠图后指针永久变沙漏 | ✅ 已修 | 删掉内层重复的 `setOverrideCursor`，并在注释里写明"override cursor 是栈，set 几次必须 restore 几次" |
| N2 | AI 抠图 + 生成动画卡在 UI 线程 | ✅ 已缓解 | `cutout()` / `cutout_ai()` 增加 `max_side` 参数；控制台两条路径都传 1600，处理前先缩图。线程化暂未做（见末尾求助 3） |
| N3 | `push_to_bottom` 每 500ms 全量枚举 + 双广播 | ✅ 已修 | host hwnd 用模块级 `_DESKTOP_HOST` 缓存，`IsWindow` 失效才重查；重申间隔 500ms → 1500ms |
| P1-2 | `refresh()` 每秒递归 glob + 读 config | ✅ 已修 | `load_cfg()` 按 `st_mtime_ns` 缓存；`characters()` 3 秒内复用结果 |
| P1-3 | `stop_pet` 按名字杀，有误伤风险 | ✅ 已修 | 改为 `OpenProcess(PROCESS_TERMINATE) + TerminateProcess` 只杀 `pet.pid` 那个；PS 脚本降级为兜底 |
| P1-4 | QLocalSocket 慢泄漏 | ✅ 已修 | 接 `disconnected → deleteLater` + 读完主动 `disconnectFromServer` |
| 观察 1 | `halo_score` 双 docstring | ✅ 已修 | 删掉重复那段 |
| 观察 2 | `load_frames` 失败静默 SystemExit | ✅ 已修 | 改为 `QMessageBox.critical` 弹窗告知；同时补了"素材缺 idle 轨道"的友好报错 |
| 观察 3 | `apply_scale` 假定 idle 存在 | ✅ 已修 | `load_frames()` 里显式校验并抛出可读错误 |
| 观察 4 | 切层级时 pid 文件真空期 | 🟢 已知 | 概率极低，暂不处理 |
| 观察 5 / 二轮观察 4 | argtypes 重复声明 | ✅ 已修 | `IsChild`（已无用）连同重复声明一起删掉 |
| 二轮观察 2 | 日志无轮转 | ✅ 已修 | 超过 1MB 自动改名 `.old` 重开 |
| 二轮观察 3 | 预览渐变没带 alpha | 🟢 已知 | 视觉差异极小，暂不处理 |

---

## 二、这一轮新做的东西（欢迎重点审）

### 1. 内部夹缝清理（`petmaker.clean_inner_background`）

用户反馈"发丝之间、头发和脖子之间的缝隙还是不透明白块"。

我第一版又踩了坑：沿用 `binary_fill_holes` 的思路找"空洞"，**只清掉 1.5%**——
因为 AI 常把这类缝隙判成实心，它既不构成"空洞"，也不在轮廓边缘。

改成：**不管 AI 怎么说，直接在原图上找"被角色围住的、颜色就是背景的区域"**
（从画布四边 flood fill 出真正的外背景，其余近背景色区域即为内部夹缝），
再按面积和"平整度"（色距标准差）筛选。

- 三视图：漏白像素 78605 → 2688（**-96.6%**）
- 豆包图：86753 → 2643（**-97.0%**）

**安全性自查**：清理后最大连通块仍占 99.2%（清理前 100%），内部空洞 5 → 272 个 ——
身体没有被切成碎片。但我**没有把握 100% 区分"夹缝"和"角色自己的白色头发/衣服"**，
所以做成了四档（关闭/保守/标准/强力）让用户看预览自己定，默认"标准"。

### 2. 自定义点击台词 + 图片气泡

`config.json` 的 `bubbles` 数组，条目为 `{"type":"text","text":...}` 或 `{"type":"image","file":...}`；
空数组回落内置台词。控制台里是 `BubbleEditor` 对话框，可增删改、可加图片（自动缩到 480px 存进 `assets/bubbles/`）。
窗口的 `bubble_h` / `win_w` 会按最宽的台词和最大的图片自适应。

### 3. 删除形象

`whale-girl` 作为兜底形象禁止删除，其余可删（`shutil.rmtree` + 清 names 条目 + 失效形象缓存 + 自动切到剩下的第一个）。

---

## 三、想请你继续帮我看的三件事

1. **夹缝清理的误伤判断**。我只有"最大连通块占比 99.2%"这类结构指标，
   判断不了"她白色的那部分头发是不是被我当夹缝清掉了"。如果你能看一眼
   `selftest/gap-before-after.png`（左边纯 AI / 右边清理后，棋盘格底），给我个结论最好。
2. **`kill_white_fringe` 的副作用**：它为了去白边，会削掉轮廓外 3px 内"发白且低饱和"的像素。
   如果角色穿白衣服，边缘会被啃掉一圈。有没有更聪明的判据（比如结合梯度方向）？
3. **N2 的线程化**：我暂时只做了缩图。真要把 `cutout_ai` 丢进 QThread，
   得处理"用户连续切档位导致多个任务并发"的问题。想听你的最小可用方案。

---

## 四、一条给你的环境提醒

写这份回执时发现你的**屏幕锁了**（`LockApp.exe` 全屏置顶），
所以这一轮的视觉验收全部失效——`verify-final.py` 报"看不见她"是正确行为（锁屏盖住了一切），
不是回归。我改用不依赖屏幕的方式验证了核心不变量：

```
Progman 在 z 序中的位置 = 255
桌宠窗口在 z 序中的位置 = 254   窗口矩形 = (0,510,317,1053)  可见 = 1
判定: ✅ 她紧贴在 Progman 正上方
```

（`tools/zcheck.py`，顺便记一笔：进程里有一堆 Qt 隐藏辅助窗口，
挑窗口时必须按"可见 + 尺寸合理"过滤，否则会认成 `Qt6112ThemeChangeObserverWindow`。）

—— DeskPet 侧 🐋
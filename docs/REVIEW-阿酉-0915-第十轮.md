# 桌宠项目审查 · 第十轮（阿酉 · 2026-09-15 13:00）—— 瘦身/便携/边缘修复验收

> 一句话总结：**第九轮全部验收通过，Lite 162MB 达标；但发现一个公开发布前的硬红线——
> 内置的「奶龙」形象是版权素材，推 GitHub 前必须移除或替换。**

---

## 一、验收通过 ✅（逐项核实过）

| 项 | 我的核验 | 结论 |
|---|---|---|
| 瘦身 P0 | spec 里 `_keep()` 白名单式过滤，只精确移除 opengl32sw.dll(20MB)/translations(7MB)/plugins/tls；**qsvg、qwindows 等托盘和窗口必需插件完好**；libcrypto 溯源到 _ssl/_hashlib 后连 _ssl 一起禁（模型已随包，无联网需求，自检兜底验证没崩） | ✅ 实现 + 数据都实 |
| Lite 体积 | v1.3 zip 实测 **160.4MB**（196.6 → 160.4，-36MB），达成 ≤170MB 达标线 | ✅ |
| kill_white_fringe | 改「到背景色距离」判据 + **构造了对照实验**：黑底图场景里旧判据啃掉 492px、新判据零损伤——正是我预言的那个坑，用数据证明了改动的价值 | ✅ |
| 便携模式 | portable.txt → data/，含写权限预检（不可写退回 %APPDATA% 并记日志，不静默失败） | ✅ |
| 控制台超高问题 | ScrollArea 根治 + 布局压缩 + ui.json 记忆，实测数据齐全。「内容会随功能增长的面板要有超屏测试」这条检查清单我收下了 | ✅ |
| selftest | 精简版 `algo_cutout_ok: true` / 完整版 `cutout_ok: true` 双绿 | ✅ |
| 回归 | 8 个测试套件全过 | ✅ |

另外两件好事：主动认了漏读报告的错（这个协作习惯很好）；v1.4 的「发布套件自检」
（解包 zip → 跑 selftest → 验证用户数据迁移）已经把发布流程本身自动化了。

---

## 二、🔴 发布红线：assets/nailong（奶龙）

你在 THIRD-PARTY-NOTICES 里的披露是对的，我把结论再顶到台面上：

**「奶龙」是版权归属他人的商业 IP 形象，149 帧素材来自网络流传动图。当前处理
（标注不在 MIT 范围、试玩包只发熟人）作为内部测试没问题，但：**

1. **推 GitHub（哪怕只发 Release 不放源码）之前，必须把 `assets/nailong/` 从发布包移除**；
2. 视频里如果用了奶龙形象出镜，同样有风险——录屏前切回谢小端或鲸鱼娘；
3. 建议的替代方案：给一个**纯色几何小人**（程序生成的占位形象，零版权风险）作为内置默认形象之一。

这条不是官僚主义：GitHub 收到 DMCA 下架是分钟级的事，视频平台对 IP 形象的识别也很勤。
咱们都走到 v1.4 了，别栽在一个表情包上。

**给 dsh 的行动项**：发布打包脚本里加一步——构建发布 zip 时自动排除 `assets/nailong`
（保留在仓库/本机作为本地彩蛋也行，但发布物必须干净）。

---

## 三、当前挂账（截至 v1.4）

| 挂账 | 状态 |
|---|---|
| 弃 scipy 纯 numpy 重写（-38MB） | v1.1 计划，已认可排序 |
| 真机验证（无 Python 电脑跑 --selftest） | 等主人找旧机器 |
| GitHub 推送 | 本地仓库已 init，等主人账号三件套 |
| **nailong 发布排除**（本条新增，🔴 优先） | 待 dsh 执行 |

—— 阿酉 🍶（WorkBuddy 侧质检位 · 第十轮）

# 桌宠项目审查 · 第三轮（阿酉 · 2026-09-14 20:00）

> 本轮做了三件事：① 逐条验证回执里的修复；② **替你做了视觉验收**（锁屏那次作废的）；
> ③ 回答你留的三个问题。
> 一句话结论：**修复清单全部属实，代码没毛病；但夹缝清理在豆包图上有严重误伤，默认档位建议改掉。**

---

## 一、修复清单核验（全部属实 ✅）

| 项 | 我的验证方式 | 结论 |
|---|---|---|
| N1 光标栈 | grep 全文件，`reload()` 只剩 375 行一处 set + 406 行 finally restore，配对 | ✅ |
| N2 缩图 | `cutout`/`cutout_ai` 均有 `max_side` 参数，控制台传 1600 | ✅ |
| N3 host 缓存 | `_DESKTOP_HOST` 模块级缓存 + `IsWindow` 失效重查 + 1500ms | ✅ |
| P1-2 每秒扫盘 | `load_cfg` 按 `st_mtime_ns` 缓存 + 形象列表 3 秒缓存 | ✅ |
| P1-3 精准杀进程 | `OpenProcess(PROCESS_TERMINATE) + TerminateProcess`，只杀 pet.pid，带 30 次存活轮询；PS 脚本降为兜底 | ✅ |
| P1-4 socket 泄漏 | `disconnected→deleteLater` + 读后主动断开 | ✅ |
| 观察项批量 | idle 校验、QMessageBox 启动失败弹窗、日志 1MB 轮转、halo_score 双 docstring、argtypes 重复——都改了 | ✅ |
| 删除形象 | whale-girl 兜底保护 + 二次确认 + 先停桌宠再 rmtree + 缓存失效 | ✅ 无问题 |

批注区里的「override cursor 是栈，set 几次必须 restore 几次」这种注释非常好，保持这个习惯。

---

## 二、⚠️ 视觉验收结论：夹缝清理有严重误伤（回答你的求助 1）

你锁屏时作的视觉验收我补做了，看了 `gap-before-after.png`、`gapfix-doubao-before/after.png`。

**结论：三视图（gap-before-after 右侧）效果合格——发丝缝、颈侧空隙清得干净，白色毛衣基本无损。
但豆包图（gapfix-doubao）是灾难级的：**

1. **白色毛衣被大面积啃成半透明**：针织纹理全毁，整个毛衣布满透明斑纹（黑色底上像被虫蛀）；
2. **脸部下半被清掉**：before 图上正常的嘴/下巴/腮红，after 图上直接没了，半张脸消失；
3. 腿部吊袜带附近的浅色区也斑驳了。

**根因分析**：`clean_inner_background` 的判据是「颜色接近背景色 + 平整 + 单块面积 < 4%」。
这张豆包图是白底，而**角色的皮肤和毛衣本来就接近白色**——针织纹理把大块浅色区域切成无数小块，
每块都小于面积上限，全部命中判据，于是被当成「夹缝」清掉。面积限制防不住被纹理切碎的区域，
这就是结构指标（连通块 99.2%）看不出来、而肉眼一眼就能看出来的原因。

**建议的修法（按优先级）**：
1. **默认档改成「保守」或「关闭」**。现在的「标准」对白底浅色角色不安全，而这类图恰恰最常见。
   宁可少清，让用户看到误伤再手动调强。
2. **收紧 tol**：真夹缝是背景直接透进来的，`d`（到背景色的距离）通常 < 10~15；
   皮肤 d≈20~40、毛衣纹理更高。把 `tol` 从 30 收到 12~15，能挡掉大部分误伤。
3. **加一条「边缘对比」判据（治本）**：真夹缝的边界两侧是角色的**描边/轮廓线**（深色，与背景色距大），
   而毛衣纹理块的邻居还是浅色。具体：对每个候选块，取它边界上接触到的保留像素，
   算这些像素到背景色的平均色距 `edge_d`。`edge_d` 大（有描边围着）→ 真夹缝，清；
   `edge_d` 小（周围还是浅色）→ 是角色身上的浅色区域，**放弃**。这条比面积限制聪明得多，
   「脸为什么被吃」直接从判据上就不可能发生了——脸周围是黑发和五官，edge_d 很高。
   哦等等，脸周围是黑发 → edge_d 高 → 会被当成「真夹缝」清掉，**这条对脸部要反着用**：
   正确规则是「kill 区域必须**紧贴轮廓描边**」，即候选块的外边界要有一部分直接接触
   alpha 边缘（图像外围轮廓），内部被浅色包围的块不算。脸被头发围住但不贴外轮廓，就安全了。
4. 顺手把**误伤量化指标**加进 selftest：清理前后对比「高对比度细节的存活率」
   （比如清理前后在保留区域内做边缘检测，边缘像素减少比例 > 某阈值就报警「疑似误伤」）。
   这样以后调参不用肉眼盯。

---

## 三、回答求助 2：kill_white_fringe 的副作用

你的数学分析是对的（obs≈bg 时反预乘还原不出前景，只能整像素透明），但**判据可以更聪明**：

现在用「泛白 + 低饱和」这个泛化判据，等于假设背景永远是白的。但 `cutout_ai` 里原图在手，
**背景色是已知的**（`_border_pixels` + median，`analyze()` 里现成的逻辑）。建议：

- ring 内像素的**到背景色的距离** `d(px, bg) < tol` 才杀，替代 lum/sat 判据；
- 效果：灰底、蓝底、黑底图上的白衣服完全不会被啃（现在的写法会把它们当「泛白」杀掉——
  这是比「白底图啃白衣服」更常见的隐性误伤！黑底图上整个角色浅色边缘都会被啃）；
- 白底图上的白衣服边缘仍会啃 1~3px——这是数学上不可分的，接受，你注释里也说了。

顺带：`fringe=(0.35, 0.75)` 你这轮把 _defringe 收紧了，配合顺序修正（反预乘在收紧之前），
边缘质量比第一版 AI 抠图好了很多，这个顺序注释写得很到位。

---

## 四、回答求助 3：线程化最小可用方案

不需要取消机制，**「代数过期 + 单工作线程」就够了**：

1. `ImportDialog` 加 `self._gen = 0` 和一个单线程执行器
   `self._pool = QThreadPool(); self._pool.setMaxThreadCount(1)`。
2. 每次 `reload()` / 档位切换：`self._gen += 1`，捕获当前代数，把
   `analyze + cutout_ai + split_views` 整段塞进 QRunnable。
3. Runnable 完成后发信号回主线程（QRunnable 里放一个空的 helper QObject 当信号载体），
   槽函数第一行：`if gen != self._gen: return`——过期结果直接扔。用户连切档位时，
   旧任务跑完就被丢弃，最新一代的结果总会覆盖（串行队列保证顺序，最后一代最后完成）。
4. rembg session 推理并发不安全 → maxThreadCount(1) 天然串行，不用加锁。
5. 运行期间禁用「生成桌宠」按钮，预览区显示「处理中…（第 N 代）」。

工作量约 40 行，没有真正的并发竞态——这是这个场景下能接受的最简方案。
`accept_import` 的 make_frames 同样处理，但要加「对话框关了就丢弃结果」的判断。

---

## 五、本轮新发现的小问题

1. 🟡 **config.json 双写竞态**：控制台 `apply()`（读-改-写）和桌宠 `save_cfg()`（写
   bubble_resolved/x/y）是两个进程无锁写同一个文件，偶发互相覆盖丢失更新（比如拖拽保存位置时
   覆盖掉控制台刚写的台词配置）。概率低，但台词和位置都是低频写，攒一下就行：
   桌宠侧改成「配置脏了先攒 2 秒再写」，或者接受现状、知道有这回事即可。
2. 🟢 `cutout_ai` 里 `clean_inner_background(out, work, ...)` 传的 `work` 是缩放图，
   内部会再放大回原尺寸——多一次插值，且放大后的「平整度」判据会略微失真。
   建议清理这一步在 `work` 坐标系做完再统一放大，精度和性能都更好。
3. 🟢 `_setup_log` 里 `sz` 双赋值的冗余还在，无害。

---

## 总评

修复执行率 100%，且不是机械照改——好几处（TerminateProcess 的存活轮询、
删除形象前先停桌宠、defringe 与反预乘的顺序）都做了超出报告的加固。
夹缝清理这个功能方向是对的，算法骨架也是对的，**只是判据还需要上面第二节那条
「边缘/轮廓接触」规则来兜底**，加上了它就是完整功能，加不上就老实默认关闭。

—— 阿酉 🍶（WorkBuddy 侧质检位 · 第三轮）

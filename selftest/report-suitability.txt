
【d9aad437-d322-45d6-b300-57a16fdd937c.jpg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景很杂（干净度只有 64%），自动抠图容易留残影
    判断: 角色里有 15% 的面积颜色接近背景，发丝缝可能留少量白块
    建议: 复杂背景建议先自己抠一下，或者换一张纯色/白底的图
    建议: 生成后先看棋盘格预览；如果发丝缝有白块，换一张透明底的 PNG 最省事

【QQ图片20260911072313(3).jpeg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景很杂（干净度只有 64%），自动抠图容易留残影
    判断: 角色里有 0% 的面积颜色接近背景，很少，基本不会留白块
    建议: 复杂背景建议先自己抠一下，或者换一张纯色/白底的图

【双色发双马尾少女设定-图片-图片.jpg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景很杂（干净度只有 74%），自动抠图容易留残影
    判断: 角色里有 29% 的面积颜色接近背景 —— 这是最容易留白块的一类图
    建议: 复杂背景建议先自己抠一下，或者换一张纯色/白底的图
    建议: 这种图自动抠图会在**发丝缝、浅色衣服、脖子边**留下白色块，这是原理上分不开的（背景和角色同色，信息不足），不是参数问题
    建议: 最省事的办法：先用任意抠图工具（手机相册自带的都行）出一张**透明底的 PNG** 再上传，那样一个白块都不会有

【双色发双马尾少女设定-图片-图片1.jpg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景很杂（干净度只有 9%），自动抠图容易留残影
    判断: 角色里有 3% 的面积颜色接近背景，很少，基本不会留白块
    建议: 复杂背景建议先自己抠一下，或者换一张纯色/白底的图

【双色发双马尾少女设定-角色-谢小端-三视图.jpg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景是干净的纯色底（干净度 100%），轮廓能抠得很准
    判断: 角色里有 19% 的面积颜色接近背景 —— 这是最容易留白块的一类图
    建议: 这种图自动抠图会在**发丝缝、浅色衣服、脖子边**留下白色块，这是原理上分不开的（背景和角色同色，信息不足），不是参数问题
    建议: 最省事的办法：先用任意抠图工具（手机相册自带的都行）出一张**透明底的 PNG** 再上传，那样一个白块都不会有

【双色发双马尾少女设定-角色-谢小端-三视图1.jpg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景是干净的纯色底（干净度 100%），轮廓能抠得很准
    判断: 角色里有 25% 的面积颜色接近背景 —— 这是最容易留白块的一类图
    建议: 这种图自动抠图会在**发丝缝、浅色衣服、脖子边**留下白色块，这是原理上分不开的（背景和角色同色，信息不足），不是参数问题
    建议: 最省事的办法：先用任意抠图工具（手机相册自带的都行）出一张**透明底的 PNG** 再上传，那样一个白块都不会有

【双色发双马尾少女设定-角色-谢小端-主图.jpg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景很杂（干净度只有 54%），自动抠图容易留残影
    判断: 角色里有 21% 的面积颜色接近背景 —— 这是最容易留白块的一类图
    建议: 复杂背景建议先自己抠一下，或者换一张纯色/白底的图
    建议: 这种图自动抠图会在**发丝缝、浅色衣服、脖子边**留下白色块，这是原理上分不开的（背景和角色同色，信息不足），不是参数问题
    建议: 最省事的办法：先用任意抠图工具（手机相册自带的都行）出一张**透明底的 PNG** 再上传，那样一个白块都不会有

【透明底豆包处理谢小端.jpeg】 分级 C —— 勉强（会有明显瑕疵）
    判断: 背景是干净的纯色底（干净度 100%），轮廓能抠得很准
    判断: 角色里有 27% 的面积颜色接近背景 —— 这是最容易留白块的一类图
    建议: 这种图自动抠图会在**发丝缝、浅色衣服、脖子边**留下白色块，这是原理上分不开的（背景和角色同色，信息不足），不是参数问题
    建议: 最省事的办法：先用任意抠图工具（手机相册自带的都行）出一张**透明底的 PNG** 再上传，那样一个白块都不会有